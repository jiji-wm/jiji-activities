# `niri-activities` design — Phase 3 of activities

Implementer-grade design for the user-facing CLI of KDE-style Activities on niri. Owns Phase 3 of the activities workstream; takes over from §8 of the compositor DD (`~/projects/desktop/de/niri/docs/activities/design.md`), which now redirects here.

This DD is the source of truth for everything user-facing: subcommand surface, error model, exit codes, IPC strategy, picker integration, output formats, test strategy. The compositor DD remains the source of truth for IPC types and compositor-side semantics.

---

## 1. Goal and scope

`niri-activities` is the CLI surface for Activities — a workspace-grouping abstraction landed compositor-side in Phases 1–2. Each invocation makes one or more sequential niri IPC calls, then exits. **No daemon, no persistent state, no event-stream subscription** in v1; those would be justified only by a UX bottleneck the per-invocation model can't meet (live activity indicator in a panel, pre-loaded picker state). Pickers (`fuzzel` for single-select, `rofi` for multi-select per §4.4) are external binaries spawned per invocation — same Unix-philosophy idiom as the rest of the niri ecosystem (anyrun, niri-ror, etc.); no Rust GUI code in this crate.

**Picker integration is PoC-quality in v1.** Both the fuzzel-for-single-select and rofi-for-multi-select integrations are first cuts focused on getting the binary functional end-to-end against the fork's IPC. The known UX seams (rofi pre-marker prefix workaround in §4.4, two-tier picker mental model, sentinel-row encoding for bulk actions) are documented but not polished; expect a v2 picker pass once real usage surfaces what actually limits the UX. This caveat applies to **every** picker-using subcommand (`switch`, `move-window`, `move-workspace`, `assign-workspace`), not just multi-select — fuzzel is "the one we have" rather than "the one we'd design."

Out of scope for v1, parked for v2: daemon mode, D-Bus interface, save-to-config-on-exit semantics beyond the explicit `save` subcommand, picker fallbacks beyond fuzzel/rofi (yad / zenity / built-in layer-shell parked unless distros lacking rofi 2.0+ become a real constraint), chezmoi-aware config editing (treated as out of scope same as every other niri-ecosystem app — see §3 `save` note).

## 2. Source-of-truth split

| Concern | Lives in | DD |
|---|---|---|
| Activities data model, IPC types, compositor layout machinery | fork at `~/projects/desktop/de/niri/niri/gajdusek/` | `~/projects/desktop/de/niri/docs/activities/design.md` |
| User-facing CLI surface, error model, picker integration | this repo | this file |

The compositor DD exposes `Request::Activities`, `Action::SwitchActivity`, `Action::CreateActivity`, etc. via the fork's `niri-ipc` crate. This DD consumes those types; it never invents them. When the compositor DD lands a new action, this DD opens a sub-phase that wraps it.

## 3. CLI surface

Lifted from compositor DD §8.2; refined here per the error/exit-code spec in §4.1. Each subcommand documents its IPC call(s), success output, and error mapping.

### `niri-activities switch [<name>]`
- IPC: `Action::SwitchActivity { name }` (named) or fuzzel picker → same action (no arg).
- Success: silent, exit 0.
- Errors: unknown name → `ActivityNotFound` (66); already-active → no-op exit 0. Already-active is indistinguishable from newly-switched at the IPC layer: the compositor's layout-side `switch_activity` silently early-returns and the dispatcher still replies `Response::Handled`. The CLI relies on this; no client-side already-active probe.
- *Picker UX is PoC-quality in v1 (§1 caveat); fuzzel is "the one we have." A v2 redesign pass is parked in Appendix B.*

### `niri-activities switch-previous` (alias: `toggle`)
- IPC: `Action::SwitchActivityPrevious`.
- Success: silent, exit 0. Errors: none specific.

### `niri-activities move-window [<name>]`
- Composes existing primitives — there is no `Action::MoveWindowToActivity` and there never will be. The compositor DD §4.3 (line 511 in `~/projects/desktop/de/niri/docs/activities/design.md`) carries an explicit non-inclusion comment: in the workspace-as-atom model, activity membership is a workspace-level property, so the external tool must materialise "window → activity" out of "window → workspace → activity". The CLI resolves the destination activity's trailing-empty workspace on the focused output and dispatches `Action::MoveWindowToWorkspace { reference: WorkspaceReferenceArg::Id(...), window_id: None, focus: ... }` against it. Operates on focused window.
- Picker variant when no arg.
- **Destination-workspace resolution.** One `Request::Workspaces` query suffices for the happy path: filter by `activities.contains(target_activity_id) && output == focused_output`, pick max-idx where `active_window_id.is_none()`. That's the trailing-empty workspace the compositor maintains under §5.4 of the compositor DD — **but only for the current activity on a connected output**; non-current activities have no auto-materialised trailing-empty on the focused output. The focused output is read from the same `Workspaces` reply (entry with `is_focused: true`), saving one round-trip vs. a separate `Request::FocusedOutput`.
- **Non-current-activity fallback.** When the filter returns empty (target activity has no workspace on the focused output), the architect spec for Phase 3.6 box 2 settles the fallback shape. Likely candidate: `MoveWindowToWorkspaceDown` (creates a fresh empty under the active activity), then `MoveWorkspaceToActivity { workspace: Some(Id(...)), activity: Name(target), focus: false }` migrates that workspace into the target. Two IPC calls instead of one, but reuses existing variants only.

### `niri-activities move-window-here`
- Single-stage picker degenerate of `move-window` pinned to the currently-active activity. No `<name>` arg — the target activity is always active. Picker presents workspaces in the active activity on the focused output; `« New workspace »` is appended as the final row. Selecting a workspace dispatches `Action::MoveWindowToWorkspace { reference: WorkspaceReferenceArg::Id(_), window_id: None, focus: false }`. Selecting `« New workspace »` dispatches `Action::MoveWindowToWorkspaceDown {}` (creates a trailing workspace under the active activity on the focused output) followed by a `Request::Workspaces` round-trip to resolve the new workspace id, then dispatches `Action::MoveWindowToWorkspace` against it. Three IPC calls in the `« New workspace »` path; one in the named-workspace path.
- Cancellation → exit 0 silently.
- *Picker UX is PoC-quality in v1 (§1 caveat). The two-stage flow in `move-window` (no arg) and the one-stage flow here share the §4.4 single-select fuzzel contract.*

### `niri-activities move-workspace [<name>]`
- IPC: `Action::MoveWorkspaceToActivity { name }`. Operates on focused workspace.
- Picker variant when no arg.
- CLI-side defaults (not fork-side defaults): `workspace: None` (resolves to focused workspace at dispatch time) and `focus: false` (move-only verb; focusing the destination is a separate gesture). `focus: false` distinguishes "move" from "move + switch."

### `niri-activities assign-workspace`
- Spawns `rofi -dmenu -multi-select` (per §4.4) with the activity list, the focused workspace's current memberships pre-marked. The user toggles rows; bulk actions (`« Select all »` / `« Only one… »`) are sentinel rows. Confirm commits the result via `Action::SetWorkspaceActivities`; cancel exits with no changes. Exit 0 either way.
- The `« Only one… »` sentinel chains a follow-up single-select rofi picker — the activity selected there becomes the sole membership. Cancellation of the chained picker exits 0 with no IPC dispatched. Implements the "unassign from all except this one" requirement.
- *Picker UX is PoC-quality in v1 (§1 caveat); the rofi pre-marker prefix workaround in §4.4 is the most visible seam.*

### `niri-activities create <name>`
- IPC: `Action::CreateActivity { name }`. Errors: name collision → `EX_CANTCREAT` (73).

### `niri-activities remove <name>`
- IPC: `Action::RemoveActivity { name }`. Errors: unknown name → `ActivityNotFound` (66); attempting to remove a config-declared activity surfaces the compositor's error verbatim.

### `niri-activities save <name>`
- Not an IPC call. Edits the user's `config.kdl` (appending `activity "name"`), then `Action::ReloadConfig`. Uses the `kdl` crate's KDL-v1 parser to preserve user-config formatting; falls back to `CliError::ConfigEdit` on parse error.
- **Chezmoi (or any other dotfiles manager) is out of scope.** `save` writes to `$XDG_CONFIG_HOME/niri/config.kdl` directly, the same way every other niri-ecosystem app edits its own config. Users on chezmoi-managed setups must `chezmoi re-add` (or equivalent) after `save` to keep their dotfiles repo in sync — same coupling as for any other tool that edits niri/waybar/swaync configs. Detecting and integrating with chezmoi would add a runtime dep and a coupling we don't want; documented here as a known limitation rather than fixed.

### `niri-activities list [--json | --format=<spec>]`
- IPC: `Request::Activities` + `Request::Workspaces` + `Request::Windows` (three sequential calls per invocation; client-side join populates the per-activity workspace list and `window_count` documented in §4.5 — the wire-level `Activity` carries neither). Output format per §4.5.

## 4. Architectural decisions

Each subsection has a **Proposed:** lead sentence stating the agent recommendation. Phase 3.0's checkboxes ratify (or amend) these proposals before any implementation begins.

### 4.1 Error model and exit codes

**Proposed:** propagate via `anyhow::Error`; map to `sysexits.h`-style codes at the `main()` boundary through a typed inner error enum.

| Code | sysexits constant | Trigger |
|---|---|---|
| 0 | EX_OK | success, including silent picker cancellation |
| 64 | EX_USAGE | clap argument errors, unknown subcommand |
| 65 | EX_DATAERR | malformed IPC response: wrong `Response` variant (`MalformedResponseSource::WrongVariant`), wire JSON decode failure (`Decode`), or compositor returned `Reply::Err(String)` (`Server`) |
| 66 | EX_NOINPUT | activity name not found (matched against the compositor's wire error string `"activity not found"`; any other `Reply::Err` payload routes to 65 via `MalformedResponse::Server`) |
| 69 | EX_UNAVAILABLE | `$NIRI_SOCKET` unset / connect refused (`CliError::SocketUnavailable`); OR picker binary missing / non-`NotFound` `$PATH`-walk error (`CliError::PickerUnavailable`) |
| 70 | EX_SOFTWARE | panic, programming-error path (a stub left unimplemented, etc.) |
| 73 | EX_CANTCREAT | create activity failed (name collision) (`CliError::CantCreate`); OR config-edit failure — KDL parse error, atomic write failure, missing home dir (`CliError::ConfigEdit(io::Error)`) |

**Picker cancellation is exit 0**, not an error: fuzzel exiting non-zero with empty stdout means the user backed out, which is a normal outcome. Only IPC errors, type-mismatch errors, and explicit failures map to non-zero.

`anyhow::Error` carries chained context for stderr; the `main()` dispatcher does `match err.downcast_ref::<CliError>()` against a typed enum (`SocketUnavailable`, `PickerUnavailable`, `ActivityNotFound`, `MalformedResponse`, `CantCreate`, `ConfigEdit`, ...) to pick the exit code. Fallback for un-typed errors is exit 1.

`MalformedResponse` carries a typed `MalformedResponseSource` with three variants — `Decode` (wire parse failure, holds the `serde_json::Error`), `Server` (compositor returned `Reply::Err(String)`), and `WrongVariant` (the wire parsed cleanly but the `Response` variant did not match the request that was sent). Splitting these typed-rather-than-stringified keeps the `serde_json::Error` chain reachable via `Error::source` for `{:#}` formatting, and lets stderr name the failure mode precisely (e.g. `expected Response::Activities, got Response::Workspaces(...)` for the `WrongVariant` case).

### 4.2 IPC strategy: fork's `niri-ipc` via git+rev

**Proposed:** depend on the fork's `niri-ipc` crate via a pinned git rev. Activities-specific IPC variants (`Request::Activities`, `Action::SwitchActivity`, etc.) only exist on the fork — crates.io's `niri-ipc` has none of them.

```toml
[dependencies]
niri-ipc = { git = "https://github.com/gajdusek/niri", rev = "<pin>" }
```

**niri-ipc dep stopgap (Phase 3.1):** the dep is currently `path = "../../niri/niri/gajdusek/niri-ipc"` (tracked rev: `54aee6582cbfc11b4e69fa8a602cf2653e29df4a`). The `feature/activities` branch is local-only and does not resolve via a git+rev URL. Switch to `git = "https://github.com/gajdusek/niri", rev = "<sha>"` once the branch is pushed. Pin rev rather than branch so IPC bumps are deliberate and reviewable.

**Why not shell out to `niri msg`?** Two reasons that compound:
- `niri msg` is the fork binary's own CLI, which forwards to the same IPC types we'd link. Going through a process boundary means parsing stringified JSON (brittle), and re-discovering structured errors that the typed Request/Response already gives us. We pay the brittleness cost without the abstraction win.
- `niri msg` returns text to stdout, not a typed `Response`. We'd hand-roll a parser per command and lose anyhow's context chaining.

**Why not path-depend on `../../niri/niri/gajdusek/niri-ipc`?** Path deps assume the workspace layout is identical on every machine that builds this crate. Git+rev ships everywhere with no implicit cross-repo coupling. The coordination cost (bumping a rev when the fork's IPC moves) is the same; the deployability is much better.

**Bumping the rev becomes a deliberate sub-phase.** When the fork lands a new activity-related IPC variant, this DD opens a sub-phase that bumps the rev and exposes the variant via a new subcommand or flag. Drift is detected by the test layer in §4.6 (an integration test that exercises every variant we wrap will fail to compile if the rev moves and a variant changes shape).

### 4.3 IPC client trait

**Proposed:** hide socket marshalling behind a trait so subcommand logic can be unit-tested without standing up a real socket.

```rust
pub(crate) trait NiriClient {
    fn send(&mut self, req: Request) -> Result<Response, IpcError>;
}

pub(crate) struct SocketClient { /* one-shot connection per send(), talks $NIRI_SOCKET */ }
pub(crate) struct MockClient   { canned: VecDeque<(Request, Response)> }
```

Subcommand logic takes `&mut dyn NiriClient`. Tests inject `MockClient` (with a queue of expected request/reply pairs; panics on unexpected request to surface test gaps); the binary wires `SocketClient`. This is the standard rust-CLI test pattern, and is a precondition for `assert_cmd` to be useful — every test would otherwise need a live niri or a Unix-socket fixture.

`MockClient::expect(req, reply)` and `assert_consumed_in_order()` form the test-level contract; the trait surface stays minimal (one method).

### 4.4 Picker contracts (two-tier: fuzzel for single-select, rofi for multi-select)

**Proposed:** two external pickers, chosen per subcommand by the shape of the selection. Both spawned per invocation via `std::process::Command`; no Rust GUI code in this crate.

> **PoC-quality caveat (v1).** Both integrations are first cuts. The fuzzel calls are minimal stdin/stdout pipes; the rofi multi-select integration uses a `[x] `/`[ ] ` prefix workaround (§4.4 "Pre-check current memberships" below) because rofi mainline does not yet support multi-row pre-check natively (upstream tracking: PR #1809 against `davatorium/rofi`, open and unmerged as of 2026-04). A v2 picker overhaul — proper rofi pre-check once #1809 lands, *or* migration to a different launcher — is parked in Appendix B. Treat the v1 picker UX as functional, not polished.

#### Single-select subcommands → fuzzel

`switch`, `move-window`, `move-workspace` spawn `fuzzel --dmenu --prompt <subcommand-prompt>`, write items to stdin (newline-separated), read selected line from stdout. Cancellation (fuzzel exits non-zero with empty stdout) → exit 0 silently. fuzzel is the default niri-ecosystem picker (cf. anyrun, niri-ror examples).

| Subcommand (no arg) | Picker prompt | Items |
|---|---|---|
| `switch` | `Switch to activity:` | activity names, focused first |
| `move-window` | `Move window to activity:` | activity names |
| `move-workspace` | `Move workspace to activity:` | activity names |

#### Two-stage single-select picker (`move-window` no-arg)

`move-window` (no arg) opens a two-stage fuzzel flow. Stage 1 is an activity picker; stage 2 is a workspace picker scoped to the chosen activity on the focused output.

**Stage 1 — activity picker.** Prompt: `Move window to activity:`. Items: `« Current activity »` sentinel as the first row (always present regardless of picker-selected activity), then all activity names. Sentinel encodes "I want a workspace within the current activity" without exposing the active activity name at picker-open time (avoids stale-activity-name confusion if focus changes mid-picker-session).

**Sentinel resolution after Stage 1.**
- `« Current activity »` selected → resolve to the active activity (one `Request::Activities` call reads the current active id); proceed to Stage 2 with that activity.
- Named activity selected → proceed directly to Stage 2 for that activity.

**Stage 2 — workspace picker.** Prompt: `Move to workspace in <activity_name>:`. Items: workspace names for the chosen activity on the focused output; `« New workspace »` appended as the final row only if the chosen activity is the **active** activity (the compositor only guarantees a trailing-empty workspace for the active activity). Selecting `« New workspace »` → `Action::MoveWindowToWorkspaceDown {}` + round-trip `Request::Workspaces` to resolve the new id, then `Action::MoveWindowToWorkspace`. Selecting an existing workspace → single `Action::MoveWindowToWorkspace { reference: Id(_), window_id: None, focus: false }`.

**Non-current-activity with no workspaces on focused output.** If Stage 2's workspace list is empty (the selected non-current activity has no workspace on the focused output), the CLI surfaces a stderr diagnostic and exits 0 — no IPC dispatched. The trailing-empty invariant only applies for the active activity; the user must create a workspace in the target activity on the focused output themselves (e.g., via `niri-activities move-workspace`) before re-trying.

**Cancellation at either stage** → exit 0 silently; the stage-1-cancelled case closes fuzzel before Stage 2 opens.

**`Stage1Resolution` / `Stage2ResolutionWithNew` / `Stage2ResolutionLiteralOnly` enums** carry the picker outcome in typed form: `Stage1Resolution::{Sentinel, Named(String), Cancelled, Unknown(String)}`; `Stage2ResolutionWithNew::{Workspace(u64), NewWorkspace, Cancelled, Unknown(String)}`; `Stage2ResolutionLiteralOnly::{Workspace(u64), Cancelled, Unknown(String)}`. The `Unknown` variant in all three handles "picker returned a row not in items we passed" as a hard error — `MalformedResponse(Server("picker returned row not in items"))` (exit 65). Resolver functions must never fold picker-side contract violations into `Cancelled`; the `Unknown` variant is the explicit out for this case. The two Stage 2 types encode the `NewWorkspace` invariant structurally: `Stage2ResolutionWithNew` carries `NewWorkspace` and is used when the `« New workspace »` sentinel is injected (active-activity path via `run_picker` and unconditionally via `run_here`); `Stage2ResolutionLiteralOnly` structurally omits `NewWorkspace` and is used on the non-active path where the compositor's trailing-empty invariant does not apply. The `bool include_new_workspace` parameter and the former `unreachable!()` arm are gone — illegal state is unrepresentable. The `NewWorkspace ⇔ active-activity` mapping is enforced at call-site discipline in `run_picker` (active branch → with-new) rather than at the type level; see Appendix C for the `ActiveActivityId` newtype that would tighten this to structural enforcement.

#### Multi-select with bulk actions → rofi

`assign-workspace` is fundamentally a multi-select operation (per compositor DD §3.2 it edits the workspace's activity-membership set). fuzzel **does not and will not** support multi-select — issue [dnkl/fuzzel#244](https://codeberg.org/dnkl/fuzzel/issues/244) is labeled `wontfix`, no PRs, no roadmap. We do not wait on this.

**Resolution: spawn `rofi -dmenu -multi-select` for `assign-workspace`.** Mainline rofi 2.0.0 merged the `lbonn/rofi-wayland` fork; rofi is now Wayland-layer-shell-native and packaged in Debian testing/sid. Other Wayland launchers were canvassed (walker, tofi, wofi, anyrun) — none have multi-select. Custom gtk4-layer-shell popup was the previous proposal; rofi supersedes it and removes a module of Rust GTK code.

Invocation:

```sh
rofi -dmenu -multi-select \
     -p 'Activities — Ctrl+Space toggle, Enter save' \
     -ballot-selected-str '[x] ' \
     -ballot-unselected-str '[ ] '
```

The prompt explicitly names the multi-select keybind because rofi's default `Ctrl+Space` is not commonly known and a user pressing `Enter` on a row expecting to toggle would silently dismiss the picker with that single row as the selection.

Stdin: one activity name per line, plus three sentinel rows at the top (see "Bulk actions" below). Stdout: the selected lines, one per line. Cancellation (Escape, or rofi non-zero exit) → exit 0 silently, no IPC dispatched.

**Pre-check current memberships.** rofi mainline does not currently support multi-row pre-check in multi-select mode. Adjacent flags don't fit: `-selected-row` works only in single-select; `-a` marks rows as styled-active but doesn't include them in the saved selection on `Enter`. Upstream tracking is [PR #1809 (`davatorium/rofi`)](https://github.com/davatorium/rofi/pull/1809) "Pre-check boxes on multi-select", maintained by lbonn (the Wayland-fork merger) and open as of 2026-04, plus issue [#1806](https://github.com/davatorium/rofi/issues/1806) for the original request. We do not block on it.

The v1 workaround: render activity names with a `[x] ` prefix already baked into the input line for activities the workspace is currently in, and `[ ] ` for ones it is not. The user toggles via rofi's normal multi-select keybind (`Ctrl+Space`); `Enter` confirms; we parse the returned lines and strip the prefix.

**Save semantics — full replacement, not diff.** On `Enter`, the saved membership set is the literal set of rows rofi returns as selected — we do **not** XOR against the starting state. This makes the picker behave like "edit the membership set; what you check is what you save", which matches the shell-multi-select intuition and avoids a confusing two-state mental model (where the prefix would mean "starting state" and the ballot would mean "pending change," with the diff being the saved value). Consequence: the user must re-check rows that are currently in-set if they want to keep them. The `[x] ` prefix is informational — it tells the user "this row is currently in-set, so re-check it to preserve" — not load-bearing for save semantics.

This still leaves a visual seam (rofi's own ballot character via `-ballot-selected-str` looks similar to our prefix) but the parsing rule is unambiguous: returned rows = saved set. Documented as a UX nit replaceable in v2 once #1809 lands or a different launcher is chosen.

**Focused-workspace contract.** The compositor invariant is that exactly one workspace is focused at any time (enforced in `niri-ipc/src/lib.rs:1097–1099`). `assign-workspace` targets the focused workspace by reading it from a `Request::Workspaces` reply. If that reply contains zero entries with `is_focused: true`, the CLI surfaces `CliError::MalformedResponse(MalformedResponseSource::Server("no focused workspace"))` → exit 65 (`EX_DATAERR`). The `"no focused workspace"` string is a **CLI-internal synthetic message**, not a niri-compositor wire emission — it is documented in rustdoc on `assign_workspace::focused_workspace` so that future grep audits of compositor wire-string match sites skip this location. Retry-with-delay or exit-0-with-diagnostic on the invariant-breach case is deferred to v2 (Appendix B).

**Bulk actions.** Two sentinel rows at the top of the input list:

- `« Select all »` — when present in the returned selection, the saved state becomes "every activity"; any other selected rows are ignored.
- `« Only one… »` — when present in the returned selection, the picker exits and we **chain a follow-up single-select rofi** (no `-multi-select` flag) showing the activity list with no sentinel rows. The activity picked there becomes the sole membership. Cancellation of the chained picker → exit 0 silently, no IPC dispatched. This implements the "unassign from all except this one" requirement without the `Only Foo` per-row sentinel-doubling that would clutter the list.

**Sentinel precedence** when multiple sentinels appear in the returned selection (user toggled more than one before pressing Enter):

1. `« Only one… »` wins — opens chained picker; literal selection and `« Select all »` ignored.
2. `« Select all »` — all-membership; literal selection ignored.
3. Literal selection — neither sentinel returned.

**Magic-item naming.** The `« … »` Unicode brackets (U+00AB, U+00BB) are unlikely to collide with niri activity names by convention, but the compositor permits arbitrary strings. Disambiguation: at picker-open time, validate that no real activity name equals one of our sentinel strings; if collision detected, fall back to a less collision-prone name (`__niri_activities_select_all__`, `__niri_activities_only_one__`) and log a debug note. This is a corner-case guard, not the main path.

#### Why rofi over the alternatives canvassed

| Candidate | Verdict | Why |
|---|---|---|
| **fuzzel** | out | dnkl/fuzzel#244 wontfix; no multi-select on roadmap |
| **rofi 2.0+** | **chosen** | native multi-select + filter + Wayland layer-shell + Debian-packaged + parseable stdout |
| walker / tofi / wofi / anyrun | out | no multi-select; anyrun#78 unimplemented |
| zenity `--list --checklist` | out | GTK checkbox list works on Wayland but no live filter / search input |
| yad `--list --checklist --search-column=N` | viable plan B | GTK3 + Wayland + filter; documented as a v2 fallback if rofi unavailable on a target distro |
| gum `choose --no-limit` | out | TUI-only; would require spawning a terminal window |
| custom gtk4-layer-shell popup | out | superseded — rofi covers the requirement with zero new Rust GUI code |

**Runtime dep contract.** rofi is required at runtime for `assign-workspace`, the same way fuzzel is required for the single-select subcommands. Detection: at the top of the `assign-workspace` flow, `which rofi` (or equivalent) → if missing, `CliError::PickerUnavailable` → exit 69 (`EX_UNAVAILABLE`) with a stderr message naming the binary and pointing at the README's install section. `CliError::PickerUnavailable` is the typed carrier for all picker-dep failures (fuzzel and rofi alike); `CliError::SocketUnavailable` carries niri-IPC failures. Both variants share exit code 69, distinguished in Display by their prefix (`"picker unavailable:"` vs. `"niri socket unavailable:"`). README documents both `fuzzel` and `rofi` as runtime deps.

### 4.5 `list` output format

**Proposed:** three modes, controlled by mutually exclusive flags.

**Default (no flag) — human-readable plain text.** One line per activity; the focused activity is prefixed `*`, others ` `:

```
* Work       (config) [3 workspaces, 12 windows]
  Personal   (config) [2 workspaces, 5 windows]
  Gaming     (runtime) [1 workspace, 0 windows]
```

Column widths are computed from the longest name + a 2-space gutter. Truncation rule: never truncate; if a name is wider than the terminal, the line wraps via the terminal's wrap behavior (no manual truncation).

Counts pluralise on `n != 1`: `1 workspace` / `0 workspaces` / `12 workspaces`; same rule for `window` / `windows`. Zero activities → empty stdout (no header line, no trailing newline).

**`--json` — machine-readable JSON.** Schema is wrapped in a top-level object carrying a `schema_version` integer so consumers can branch on shape changes without parsing failures. Bumping `schema_version` is a breaking change; additive fields (new optional keys) keep the version constant.

```json
{
  "schema_version": 1,
  "activities": [
    {
      "name": "Work",
      "kind": "config",
      "focused": true,
      "workspaces": [
        {"id": 1, "name": "main", "sticky": false}
      ],
      "window_count": 12
    }
  ]
}
```

When the activities list is empty, `--json` emits `{"schema_version": 1, "activities": []}` (the envelope is never omitted; consumers parse one shape unconditionally). The empty-stdout zero-case applies only to the default plain output.

Consumers that don't care about versioning can read `.activities[]` directly via `jq`:

```sh
niri-activities list --json | jq -r '.activities[].name'
```

**`--format=<spec>` — comma-separated fields per line.** E.g., `--format=name,kind,focused`:

```
Work,config,true
Personal,config,false
Gaming,runtime,false
```

Recognized fields: `name`, `kind`, `focused`, `workspace_count`, `window_count`. Unknown field → `EX_USAGE`. Useful for scripting (`niri-activities list --format=name | rofi -dmenu`).

`--json` and `--format=` are mutually exclusive (clap validates). Both override the default.

### 4.6 Test strategy

**Proposed:** three layers, in increasing fixture cost.

**Pure-function unit tests** in each module — parsing, formatting, error mapping. Fastest feedback, no fixtures. Live `#[cfg(test)] mod tests` in the same source file as the function under test.

**Subcommand integration tests via `MockClient`** (per §4.3) plus `assert_cmd` for the binary boundary. (`assert_cmd` introduced in Phase 3.1 as a dev-dep alongside the binary skeleton.) Each subcommand has at minimum a golden-path test, an error-path test, and (where applicable) a picker-cancellation test. `MockClient` is wired into the binary via a thread-local override landed in Phase 3.2: `make_client()` is a `#[cfg(test)]`-aware factory — in test builds it consults a thread-local populated by `install_mock` (returning an RAII guard); in production builds the thread-local is not compiled in and `make_client()` returns a fresh `SocketClient` directly. Env-var injection was rejected because it would leak test infrastructure into production codepaths. **Asymmetry for Phase 3.3+:** `assert_cmd`-launched smoke tests cross a process boundary and cannot use the thread-local; in-process unit tests in each subcommand's `tests` module use it.

**End-to-end smoke test against a real niri**, gated `#[ignore]`. Manual run (`cargo test -- --ignored`); not part of `cargo test` default. Asserts side effects (post-action workspace state via `niri msg`) rather than process exit codes.

`cargo test --all` runs unit + MockClient integration; `cargo test -- --ignored` adds the smoke layer.

---

## 5. Phases

### Phase 3.0 — Design ratification (no code)

Each box is a human-gated decision. The architect refuses to plan Phase 3.1+ until every box is `[x]` or amended. **Proposed:** entries are agent recommendations; the human ratifies or amends in-place before the loop drives implementation.

- [x] Error model & exit codes — see §4.1. **Proposed:** anyhow + sysexits.h mapping per the table.
- [x] IPC strategy — see §4.2. **Proposed:** fork's `niri-ipc` via git+rev (pin TBD in 3.1).
- [x] IPC client trait — see §4.3. **Proposed:** `NiriClient` trait, `SocketClient` + `MockClient` impls.
- [x] Picker contracts — see §4.4. **Proposed:** two-tier external pickers — fuzzel `--dmenu` for single-select (`switch` / `move-*`); `rofi -dmenu -multi-select` for `assign-workspace` with `« Select all »` / `« Only one… »` sentinel rows for bulk actions; `« Only one… »` chains a follow-up single-select rofi for the "unassign from all except this one" path. Save semantics are full-replacement (returned rows = saved set), not diff. Rofi prompt names the multi-select keybind (`Ctrl+Space`) for discoverability. Both runtime deps documented in the README; no Rust GUI code in v1. **PoC-quality** — see §1 caveat and §4.4 PoC note; v2 picker overhaul (track rofi PR #1809 for proper pre-check, evaluate alternative launchers) parked in Appendix B.
- [x] `list` output format — see §4.5. **Proposed:** default plain / `--json` / `--format=<spec>`.
- [x] Test strategy — see §4.6. **Proposed:** unit + MockClient/assert_cmd integration + ignored e2e smoke.

### Phase 3.1 — Skeleton & error machinery

- [x] Add Cargo deps ratified in 3.0: `clap` (derive), `niri-ipc` (git+rev — pin to fork HEAD at the time the box is landed and record the rev in the commit message), `serde`, `serde_json`, `anyhow`. **Lock the rev to the actual fork HEAD; do not invent a rev.** Landed as `413b49d` (`cargo: add clap, niri-ipc, serde, anyhow deps`).
- [x] `clap`-based subcommand dispatch matching §3. Each subcommand stub prints `not implemented` to stderr and exits 70 (EX_SOFTWARE). Top-level binary still produces useful `--help`. Landed as `e7d6743` (`cli: skeleton subcommand dispatch + typed error enum`).
- [x] `CliError` enum (typed: `SocketUnavailable`, `ActivityNotFound`, `MalformedResponse`, `CantCreate`, `Usage`) with rustdoc on each variant naming the trigger condition. `main()` dispatcher maps via `downcast_ref` to exit codes per §4.1. Landed as `e7d6743`.
- [x] `--version` prints `env!("CARGO_PKG_VERSION")`. Landed as `e7d6743`.
- [x] Unit tests for the error → exit code mapping (one test per code). Pinned at 0 warnings as of `e7d6743`.

**Reviewed:** 2026-05-14 (`413b49d`, `e7d6743`). Phase 3.1 — `413b49d` adds the dependency surface (clap, niri-ipc via path stopgap, serde, serde_json, anyhow, assert_cmd + predicates as dev-deps); `e7d6743` lands the skeleton: nine-subcommand clap dispatch, six-variant `CliError` typed enum with `map_exit`, manual clap-error routing for help/version, and four `assert_cmd` integration smoke tests. Reviewed across four review aspects (general code quality, silent-failure surface, comment accuracy, test coverage) via two `/pr-review-toolkit:review-pr` passes (initial + targeted re-review of fixer's amendments); re-review was clean. **Finding worth surfacing (niri-ipc path stopgap):** `413b49d` uses `path = "../../niri/niri/gajdusek/niri-ipc"` rather than the spec's `git+rev` because `feature/activities` is local-only and a git URL would not resolve. The tracked rev (`54aee6582cbfc11b4e69fa8a602cf2653e29df4a`) is recorded in the commit body; the migration trigger is "once the branch is pushed." See §4.2 stopgap note. **Finding worth surfacing (`NotImplemented` as the sixth variant):** the DD's §4.1 table has five exit codes for the non-stub path; `e7d6743` adds `NotImplemented` (exit 70, `EX_SOFTWARE`) as the sixth variant powering the stub arms. This variant is expected to disappear as sub-phases land real implementations; tracking it here so Phase 3.6's cleanup reviewer knows to look for lingering stubs. **Finding worth surfacing (fuzzel-cancellation exit 0 contract):** §4.1's "picker cancellation is exit 0" rule has no variant in the Phase 3.1 enum — the non-IPC cancellation path will be represented as `Ok(())` return from the picker fn (no `CliError` involved). Confirmed at review; no follow-up needed for Phase 3.1, but Phase 3.5 must not introduce a `PickerCancelled` variant that would exit non-zero. Post-review fixes squashed into `e7d6743`: three escalations triaged to Appendix C (typed source carriers on `SocketUnavailable`/`MalformedResponse`, struct variants for all five string-shaped variants, `map_exit` chain-walk doc trim); `toggle` alias routing confirmed clean (`toggle_alias_routes_to_switch_previous` test). DD amended in this commit: §3 `move-window` IPC-gap note; §4.2 stopgap paragraph; §4.6 `assert_cmd` introduction parenthetical; Phase 3.1 boxes flipped to `[x]` with landing notes; Box 5 wording updated to "Pinned at 0 warnings as of `e7d6743`"; Appendix A extended with Phase 3.1 files; Appendix C opened with four entries (fork-side `MoveWindowToActivity`, typed source carriers, struct variants, chain-walk doc trim). Same 16 tests green (11 before Phase 3.1, delta +5: `socket_unavailable_survives_context_wrap`, `cli_error_survives_context_wrap_in_alternate_format`, `toggle_alias_routes_to_switch_previous`, `no_args_exits_64`, `list_json_and_format_conflict_exits_64`); `cargo clippy --all --all-targets` 0 warnings — Phase 3.1 commits pin the project clippy baseline at zero. Proceed to Phase 3.2 (IPC adapter) with the reviewed base.

### Phase 3.2 — IPC adapter

- [x] `NiriClient` trait + `SocketClient` impl per §4.3. `SocketClient::send` connects to `$NIRI_SOCKET`, sends one `Request`, awaits one `Response`. Connection per call (no persistent state). Landed as `38c2f18`.
- [x] `MockClient` impl with a `VecDeque<(Request, Response)>` queue. `expect(req, reply)` enqueues; `send` panics on unexpected request to surface test gaps; `assert_consumed_in_order()` for end-of-test invariant. Landed as `38c2f18`.
- [x] Map niri-ipc transport errors: connect-refused / `$NIRI_SOCKET` unset → `SocketUnavailable`; reply-shape mismatch → `MalformedResponse`. Concrete error-variant table in the rustdoc on `IpcError`. Landed as `38c2f18`.
- [x] Wiring strategy for tests (the §4.6 open detail): thread-local injection via `make_client()` consulting a `#[cfg(test)]` thread-local. Env-var rejected. Landed as `38c2f18`.
- [x] Unit tests: `SocketClient` against a temp Unix socket fixture (std-thread `UnixListener` accepts one connection, replies with a fixed `Response`). `MockClient` panic-on-unexpected coverage. Landed as `38c2f18`.

**Reviewed:** 2026-05-14 (`38c2f18`, was `61145f2` → `c4bc5f5` → `38c2f18` across two fixer amends). Phase 3.2 — all five IPC-adapter boxes in one commit: `NiriClient` trait, `SocketClient` open-coded round-trip, `MockClient` with `Drop`-enforced queue-consumption check, thread-local test-injection, and the `IpcError` → `CliError` mapping with three new unit tests. Reviewed across five review aspects (general code quality, silent-failure surface, comment accuracy, test coverage, type design). **Finding worth surfacing (`SocketClient` open-coded round-trip):** `SocketClient::send` does not delegate to `niri_ipc::socket::Socket::send`. The upstream helper's `?` on `serde_json::from_str` converts the decode error into `io::Error`, which would collapse a malformed reply into `IpcError::Transport` (exit 69) and erase `IpcError::Decode` (exit 65). The rationale is captured in the module rustdoc and the commit body; a future refactor that restores delegation would re-collapse the 65-vs-69 distinction. **Finding worth surfacing (`Drop` guard for `MockClient`):** `MockClient` implements `Drop` with a `!std::thread::panicking()` gate; if the instance is dropped with unconsumed expectations it panics, naming the remaining request shapes. The original spec called `assert_consumed_in_order()` as an explicit end-of-test call; the `Drop` guard is an additional forward-looking defense against Phase 3.3+ tests that forget the explicit assertion. Post-review fixes squashed into `38c2f18`: typed source carriers landed — `SocketUnavailable(io::Error)` and `MalformedResponse(MalformedResponseSource { Decode(serde_json::Error), Server(String) })` replacing the Phase 3.1 `String` shapes (Appendix C entry partially resolved — see below); four `error.rs` tests amended to construct the typed shapes; env-var injection rejected and thread-local chosen with RAII guard pattern; `Mutex<()>` serialization added for env-mutating tests under Rust 2024 `unsafe { set_var }`. DD also amended in this commit: §4.1 row 65 trigger language expanded; §4.6 thread-local injection mechanism documented with Phase 3.3+ boundary asymmetry noted; Appendix A extended with `src/ipc.rs`; Appendix C typed-source-carriers entry marked partially landed. Resolves Appendix C entry: "Typed source carriers on `CliError` variants" — `SocketUnavailable(io::Error)` and `MalformedResponse(MalformedResponseSource)` landed in this commit; the "struct variants" sibling entry remains open. Same 32 tests green (was 16; delta +16: 12 new `ipc.rs` tests + 3 updated `error.rs` tests + 1 existing integration test `list_json_and_format_conflict_exits_64` that had already landed in Phase 3.1's `tests/cli.rs`; counted as 31 in an earlier tally — corrected here); `cargo clippy --all --all-targets` clean. Proceed to Phase 3.3 (`list` subcommand) with the reviewed base.

### Phase 3.3 — `list` subcommand

- [x] Plain output per §4.5. Edge cases: zero activities → empty stdout; long names → no truncation. Landed as `8fb13cf`.
- [x] `--json` output per §4.5; matches the documented schema exactly. Landed as `8fb13cf`.
- [x] `--format=<spec>` per §4.5; unknown field → `EX_USAGE`. Landed as `8fb13cf`.
- [x] `--json` and `--format=` mutually exclusive — regression test pins the clap-level `conflicts_with` rule (wiring already landed in the skeleton commit; this box adds the pinning test). Landed as `8fb13cf`.
- [x] Integration tests via `MockClient` + `assert_cmd`: golden plain output (3 activities, focused middle), golden JSON, three `--format=` variants, zero-activities plain, zero-activities JSON. Landed as `8fb13cf`.

**Reviewed:** 2026-05-14 (`8fb13cf`, was `da3cd9e` → `a955749` → `8fb13cf` across two fixer amends). Phase 3.3 — all five `list`-subcommand boxes in one squashed commit: three render paths (plain, `--json`, `--format=<spec>`), client-side join of Activities × Workspaces × Windows in fixed IPC order, five `MockClient`-driven in-process tests, and eight `assert_cmd` integration tests crossing the process boundary. Reviewed across five review aspects (general code quality, silent-failure surface, comment accuracy, test coverage, type design). **Finding worth surfacing (`CliError::OutputPipeClosed` as typed BrokenPipe sentinel):** cycle-1 originally landed stdout-write `BrokenPipe` as an `err.chain().any(io::ErrorKind::BrokenPipe)` predicate in `main.rs`. This was a silent-failure regression: the predicate would have matched `CliError::SocketUnavailable(io_err)` when the inner `io::Error` happened to be `BrokenPipe` (e.g., niri compositor crashing mid-write), suppressing the error with exit 0 instead of exit 69. Cycle-2 introduced `CliError::OutputPipeClosed` as a typed sentinel emitted only at write sites in `src/list.rs`; `main.rs` checks for this variant specifically before falling through to the general `map_exit` path. The `root_cause()` vs. `chain().any()` asymmetry between `classify_write_err` and `is_stdout_pipe_closed` is noted in Appendix C as a defensive maintenance item. **Finding worth surfacing (`MalformedResponseSource::WrongVariant` — first production use):** Phase 3.3 is the first sub-phase to construct a `WrongVariant` value in production code (`src/list.rs` raises it on each of the three IPC responses when the returned `Response` variant does not match the expected one). The `variant_name` helper called from `WrongVariant`'s context string returns `"Response::<unknown>"` for any unrecognised variant beyond the enumerated 16; the forward-compat gap is parked in Appendix C. **Finding worth surfacing (`dead_code` debt cleared):** Phase 3.3 is the first production consumer of the IPC layer; `src/ipc.rs`'s `NiriClient` trait, `IpcError` enum, and `make_client` factory no longer carry `#[allow(dead_code)]` markers. Post-review fixes squashed into `8fb13cf`: cycle-1 — 7 tests added covering `WrongVariant` on `Workspaces`/`Windows` arms, `run()` dispatch routing, zero-case `--format=` output, `WorkspaceCount`/`WindowCount` fields, duplicate-field validation; cycle-2 — ~8 further tests covering `BrokenPipe` predicate coverage, `MockClient::remaining_count` self-test, `variant_name` assertion strengthening on three existing tests, whitespace strict-contract pin, `Activities WrongVariant` context-layer assertion; `CliError::OutputPipeClosed` introduced replacing the `chain().any()` predicate; `MockClient::remaining_count()` test-only accessor added to `src/ipc.rs`. DD amended in this commit: Phase 3.2 Reviewed: test count corrected (31 → 32); Phase 3.3 boxes flipped to `[x]` with landing notes; Appendix A extended with `src/list.rs` and `src/error.rs`/`src/main.rs`/`src/ipc.rs` deltas; Appendix C extended with five new entries. Same 62 tests green (54 unit + 8 integration; was 32; delta +30 — ~+7 from cycle-1, ~+8 from cycle-2, remainder from the initial implementation); `cargo clippy --all --all-targets` 0 warnings (baseline unchanged). Proceed to Phase 3.4 (`switch <name>`) with the reviewed base.

### Phase 3.4 — `switch <name>` subcommand (no picker yet)

- [x] Dispatch IPC `Action::SwitchActivity { name }`. Landed as `8ae7384`.
- [x] Unknown name → `ActivityNotFound` → exit 66 (the compositor returns a structured error; map it). Landed as `8ae7384`.
- [x] Already-active name → no-op silently, exit 0 (verify against compositor DD §5.3 — switching to the active activity is a documented no-op). Landed as `8ae7384`.
- [x] Integration tests: golden, unknown name, already-active. Landed as `8ae7384`.

**Reviewed:** 2026-05-14 (`8ae7384`, was `899bad3` before the post-review amend). Phase 3.4 — all four `switch <name>` boxes in one squashed commit: `src/switch.rs` wires `Action::SwitchActivity { activity: Name(name) }`, routes the compositor's literal `"activity not found"` wire string to `CliError::ActivityNotFound` (exit 66), falls through on any other `Reply::Err` to `MalformedResponse(Server)` (exit 65), and maps `Response::Handled` to `Ok(())` for both the real-switch and already-active cases. Reviewed across four review aspects (general code quality, silent-failure surface, comment accuracy, test coverage). **Finding worth surfacing (synthetic-vs-real wire string discipline):** the implementer's initial `send_expect_handled` test injected `"activity switch blocked: gesture in flight"` — plausible-sounding but not a real compositor emission. The fixer replaced it with `"activity switch blocked: workspace switch gesture"` (matching the actual format template in `niri/gajdusek/src/layout/mod.rs`). Future action-returning subcommands (Phase 3.6) should cross-check test wire strings against the fork's layout-side format templates, not invent them. **Finding worth surfacing (negative-space tests for strict-equality string matches):** the CLI matches `msg == "activity not found"` exactly, but no test originally pinned that the match is strict-equality rather than a prefix or substring check. The fixer added two: suffix-with-name (`"activity not found: Work"`) and capitalized (`"Activity not found"`). Future Phase 3.6 sub-phases that pattern-match wire strings should follow the same negative-space pattern. **Finding worth surfacing (binary-boundary integration test pattern):** the DD's "integration tests" box was satisfied with a `$NIRI_SOCKET`-unset → exit 69 test (`switch_named_no_socket_exits_69` in `tests/cli.rs`). This pins the wiring direction at the process boundary without requiring a fake-socket harness — a cheap, replicable precedent for Phase 3.6. Post-review fixes squashed into `8ae7384`: two negative-space unit tests added (`not_found_suffix_routes_to_malformed`, `not_found_capitalized_routes_to_malformed`) pinning strict-equality; one binary-boundary integration test added (`switch_named_no_socket_exits_69` in `tests/cli.rs`); `send_expect_handled` other-server-error test wire string corrected to a real compositor format string. DD also amended in this commit: §3 `switch` already-active clarification added; §4.1 row 66 trigger expanded with wire-string match note; Appendix C extended with one new entry. Same 72 tests green (was 62; delta +10: +7 from initial implementation, +3 from fixer additions — 2 negative-space unit tests + 1 binary-boundary integration test); `cargo clippy --all --all-targets` clean. Proceed to Phase 3.5 (fuzzel picker) with the reviewed base.

### Phase 3.5 — fuzzel picker (single-select)

- [x] Spawn fuzzel via `std::process::Command`, pipe items to stdin (one activity per line), read stdout selection. Landed as `b11e7eb`.
- [x] Cancellation (non-zero exit + empty stdout) → exit 0 silently. Landed as `b11e7eb`.
- [x] `niri-activities switch` (no arg) opens picker, then dispatches §3.4 path with the chosen name. Landed as `b11e7eb`.
- [x] Integration test: shim binary on `PATH` overrides `fuzzel` for the test process; reads stdin, writes a fixed line to stdout, exits 0. Tests the full pipe-and-read flow. Landed as `b11e7eb`; sidecar-capture tests pinning stdin payload and prompt args landed as `5fb02c7`.
- [x] `which fuzzel` failure → `EX_UNAVAILABLE` with a stderr message naming the binary. Landed as `b11e7eb`.

**Reviewed:** 2026-05-14 (`b11e7eb`, `5fb02c7`, was `5531687` before the post-review amend). Phase 3.5 — all five fuzzel-picker boxes across two commits: `b11e7eb` introduces `src/picker.rs` (`pick_one`, `ensure_available`, `which_in_path`, `classify_output`, `names_focused_first`), wires no-arg `switch` to open the picker and dispatch the selected name via the existing §3.4 IPC path, and adds unit + `picker_shim` integration tests; `5fb02c7` adds two sidecar-capture integration tests pinning the stdin payload and `--prompt` argument at the process boundary. Reviewed across five review aspects (general code quality, silent-failure surface, comment accuracy, test coverage, type design). **Finding worth surfacing (preserve inner `io::Error` through stdin-write failure):** the initial implementation discarded the inner `io::Error` from `stdin.write_all` and `stdin.flush` failures, surfacing only a generic context string. Post-review, the error is preserved via `.context(...)` with `.with_context(|| format!(...))` so the `{:#}` chain in `main.rs` names the OS error. Future pipe-write sites should follow the same pattern. **Finding worth surfacing (kill + wait on stdin-write failure):** if `write_all` to fuzzel's stdin fails, the child process may still be running. Without `child.kill()` + `child.wait()`, the zombie lingers and the pipe fd leaks. Post-review, the failure path calls `kill()` then `wait()` before returning the error. **Finding worth surfacing (log defensive classification branches):** `classify_output`'s unreachable defensive branch (non-zero exit + non-empty stdout — fuzzel does not emit this) was originally silent. Post-review it emits a `log::warn!` so if a future fuzzel version changes the cancellation contract the branch is detectable in practice. **Finding worth surfacing (capture fuzzel stderr with `fuzzel:` prefix):** the initial implementation discarded fuzzel's stderr; a `command not found` or missing-binary message from the shell was invisible to the user. Post-review, fuzzel's stderr is captured and re-emitted with a `fuzzel:` prefix so `CliError::SocketUnavailable`'s Display correctly names the subprocess as the failure origin. **Finding worth surfacing (`.context` wrappers on picker call sites):** `switch.rs`'s call to `pick_one` and `ensure_available` initially propagated bare `CliError`; post-review both gain `.with_context(|| ...)` wrappers that chain the subcommand name so `{:#}` output identifies the call site. **Finding worth surfacing (empty PATH entry guard):** `which_in_path` iterates `$PATH` split on `:` — an empty component (leading/trailing colon, `::` run) represents `.` (current dir) by POSIX convention. Without a guard, `is_executable_file("")` resolves to the process cwd, which is a latent path-injection vector. Post-review, empty components are skipped with a comment. **Finding worth surfacing (`Fn` bound loosened from `FnOnce`):** the initial `pick_one` signature used `FnOnce` for the command factory, which prevented the retry-test patterns from reusing the closure. Loosened to `Fn` so callers are not constrained by call-count. **Finding worth surfacing (`#[derive(Debug, PartialEq, Eq)]` on `PickerOutcome`):** the initial `PickerOutcome` enum lacked `PartialEq`; tests used `matches!()` pattern guards. Post-review `Debug + PartialEq + Eq` derived so tests can use `assert_eq!` with explicit rhs, making failure messages self-documenting. Post-review fixes squashed into `b11e7eb`: preserve-inner-io-error on stdin write/flush (`src/picker.rs`); kill+wait on stdin-write failure (`src/picker.rs`); log defensive `classify_output` branch (`src/picker.rs`); capture fuzzel stderr with `fuzzel:` prefix (`src/picker.rs`); `.context` wrappers on `pick_one` and `ensure_available` call sites (`src/switch.rs`); empty `$PATH` component guard (`src/picker.rs`); `Fn` bound loosened from `FnOnce` on the command-factory parameter (`src/picker.rs`); `#[derive(Debug, PartialEq, Eq)]` added to `PickerOutcome` (`src/picker.rs`); test rename for clarity (multiple `tests/picker_shim.rs` tests renamed to match the pattern established in Phase 3.4); docstring fixes on `ensure_available` and `which_in_path` (`src/picker.rs`). `5fb02c7` adds `fuzzel_stdin_payload_reaches_shim` and `fuzzel_prompt_arg_is_switch_prompt` — each shim writes its observed inputs to a tempfile via an env-var path; the test reads and asserts, closing the regression gap where a dropped `stdin.write_all` or wrong `--prompt` string would pass the cancel/select tests undetected. Appendix C extended in this commit with three architectural escalations (I1 `CliError::PickerUnavailable` variant, C2 `is_executable_file` non-`NotFound` error swallowing, I4 empty-activities silent UX) and four low-priority cleanup items (cancel-path observability in `tests/picker_shim.rs`, `pick_one`'s unreachable stdin-pipe-missing branch, `names_focused_first` clone overhead, `PICKER_MISSING_MESSAGE` visibility). Same 91 tests green (was 72; delta +19 — +17 from `b11e7eb`, +2 from `5fb02c7`); `cargo clippy --all --all-targets` 0 warnings (zero-warning baseline held). Proceed to Phase 3.5b (rofi multi-select picker for `assign-workspace`) with the reviewed base.

### Phase 3.5.5 — picker error model + zero-case UX (post-3.5 hardening)

Lands three architectural escalations from Phase 3.5's review (Appendix C I1, C2, I4) before Phase 3.5b copies the same patterns to rofi. Distinct from 3.5b because the changes are scoped to the error model and `switch` zero-case — no rofi surface; bundling would conflate "fixing the v1 picker" with "adding multi-select." Lands as one commit.

- [x] Mint `CliError::PickerUnavailable(io::Error)` sharing exit code 69 with `SocketUnavailable`. Display: `"picker unavailable: {io}"`. `Error::source` returns the inner `io::Error`. Add `picker_unavailable_is_69` unit test paralleling `socket_unavailable_is_69`. (Closes Appendix C I1.) Landed as `5a50ef0`.
- [x] Swap the 5 picker call sites in `src/picker.rs` from `CliError::SocketUnavailable` to `CliError::PickerUnavailable` (`ensure_available`, `pick_one`'s spawn / stdin-pipe-missing / stdin-write-failure / wait-failure paths). Update `PICKER_MISSING_MESSAGE` to drop the redundant "command not found on $PATH" framing now that the variant prefix carries the cause; new text: `"fuzzel: not on $PATH (required for single-select picker)"`. Update the `fuzzel_missing_from_path_exits_69` integration test stderr substring. Landed as `5a50ef0`.
- [x] `is_executable_file` signature change: `&Path -> Result<bool, io::Error>`. `Ok(true)` for an executable regular file; `Ok(false)` only when `metadata` returned `NotFound` (canonical "keep walking" case); `Err(io)` for any other `io::Error` kind (`PermissionDenied`, `EIO`, `FilesystemLoop`, etc.). Add unit test pinning that `PermissionDenied` surfaces, not collapses. (Closes Appendix C C2.) Landed as `5a50ef0`.
- [x] `which_in_path` updates: `Ok(true)` from `is_executable_file` returns success, `Ok(false)` continues the loop, `Err(io)` short-circuits and returns the wrapped error to the caller. Add unit test using a tempdir with a chmod-`000` PATH entry (skip-on-root) confirming the inner error reaches the caller. Landed as `5a50ef0`.
- [x] `ensure_available` updates: map `which_in_path`'s non-`NotFound` errors to `PickerUnavailable(io)` preserving the original `io::Error` (not the synthetic `PICKER_MISSING_MESSAGE` constant). Update docstring to reflect the two-arm mapping (NotFound → synthetic message, anything else → preserved error). Landed as `5a50ef0`.
- [x] Empty-activities zero-case in `src/switch.rs::run_picker`: print `"niri-activities: no activities configured; nothing to switch to"` to stderr, return `Ok(())`. Rename existing unit test `run_picker_empty_activities_short_circuits_silently` → `run_picker_empty_activities_warns_and_exits_zero` for naming consistency; add an integration test of the same name in `tests/picker_shim.rs` asserting the stderr message via `assert_cmd::Command::stderr(predicate)`. (Closes Appendix C I4.) Landed as `5a50ef0`.

DD updates landed in the scribe pass: §4.1 table for code 69 unified into one row naming both `SocketUnavailable` and `PickerUnavailable` as carriers (shared exit code 69, distinguished by Display prefix); §4.4 "Runtime dep contract" paragraph expanded to name `CliError::PickerUnavailable` as the typed carrier and note the Display-prefix distinction; §4.1 prose enum updated to include `PickerUnavailable`; Appendix C entries I1, C2, I4 closed out with landing references.

**Reviewed:** 2026-05-14 (`5a50ef0`, was `7a2aa0c` → `dc6dc62` → `5a50ef0` across two fixer amends). Phase 3.5.5 — all six error-model hardening boxes in one squashed commit: `CliError::PickerUnavailable(io::Error)` minted sharing exit 69 with `SocketUnavailable`; five call sites in `src/picker.rs` swapped from `SocketUnavailable` to `PickerUnavailable`; `PICKER_MISSING_MESSAGE` reframed to omit the now-redundant "command not found on $PATH" clause; `is_executable_file` widened to `Result<bool, io::Error>` with `Ok(false)` restricted to `NotFound` and `Err(io)` for any other kind; `which_in_path` short-circuits on first non-`NotFound` stat error; `ensure_available` two-arm mapping (NotFound → synthetic message, anything else → preserved `io::Error`); `run_picker` empty-activities branch emits a one-line stderr diagnostic before exit 0; sentinel-file shim strategy added to the empty-activities integration test. Reviewed across five review aspects (general code quality, silent-failure surface, comment accuracy, test coverage, type design). **Finding worth surfacing (`running_as_root()` checks both rUID and EUID):** the chmod-000 permission-bypass guard in the new `is_executable_file_propagates_non_not_found` test reads `/proc/self/status` and skips if either the real UID or effective UID is 0 — specifically `.take(2).any(|uid| uid == "0")` to cover sudo-invoked sessions where real UID ≠ effective UID. Function name `running_as_root()` claims "running as root" but the name-governance issue is the effective UID; the defensive double-check avoids a false-positive test failure on `sudo cargo test`. Future root-skip guards should replicate this pattern rather than checking only rUID. **Finding worth surfacing (sentinel-file shims must use shell builtins, not external binaries):** `run_picker_empty_activities_warns_and_exits_zero` uses `: > "$SHIM_INVOKED"` (POSIX null-command redirection) to write the sentinel file inside the shim rather than `touch "$SHIM_INVOKED"` — `touch` is not available under `env_clear()` restricted PATH. Future shim-based integration tests that need to write files at shim invocation should use `: > "$file"` or an equivalent shell builtin, not external binaries. Post-review fixes squashed into `5a50ef0`: S1 (cycle-1) — stale `SocketUnavailable` references in comments updated to `PickerUnavailable` in `tests/picker_shim.rs` and `tests/cli.rs`; stale "command not found on $PATH" framing in `src/cli.rs` comment updated to match new `PICKER_MISSING_MESSAGE`; rustdoc on `PICKER_MISSING_MESSAGE` and `ensure_available` tightened to scope the constant to the `NotFound` arm and mention `$PATH`-unset as a distinct trigger; S2 (cycle-2) — `running_as_root()` widened to check both rUID and EUID via `/proc/self/status` `.take(2).any(|uid| uid == "0")`; sentinel-file shim strategy introduced replacing the prior approach where the cancel-path test could not distinguish "exit 0 from cancel" from "fuzzel never invoked." DD also amended in this commit: §4.1 table row 69 expanded; §4.4 "Runtime dep contract" paragraph expanded with `PickerUnavailable` symmetry note; §4.1 prose enum updated; Appendix C I1, C2, I4 closed; two new Appendix C post-landing notes added. Same 100 tests green (85 unit + 9 CLI integration + 6 `picker_shim` integration; was 91; delta +9 — `picker_unavailable_is_69`, `which_in_path_propagates_permission_denied`, `is_executable_file_propagates_non_not_found`, `is_executable_file_missing_path_is_ok_false`, `is_executable_file_non_executable_is_ok_false`, `ensure_available_not_found_synthesises_canonical_message`, `ensure_available_preserves_non_not_found_verbatim`, `which_in_path_short_circuits_before_later_match`, `run_picker_empty_activities_warns_and_exits_zero`); `cargo clippy --all --all-targets` no new warnings (zero-warning baseline held). Proceed to Phase 3.5b (rofi multi-select picker for `assign-workspace`) with the reviewed base.

### Phase 3.5b — `assign-workspace` multi-select picker (rofi)

Lands the `assign-workspace` UI per §4.4. Distinct from Phase 3.5 (fuzzel) because the picker tool, semantics (multi-select), and selection-parsing logic differ; bundling would conflate two review surfaces. Smaller than the prior gtk4-layer-shell variant of this phase — no Rust GUI code.

- [x] `src/picker/multi_select.rs` — `which rofi` detection (→ `EX_UNAVAILABLE` if missing), spawn `rofi -dmenu -multi-select -p 'Activities — Ctrl+Space toggle, Enter save' -ballot-selected-str '[x] ' -ballot-unselected-str '[ ] '`, write activity list (with `« Select all »` / `« Only one… »` sentinel rows + `[x] `/`[ ] ` pre-marker prefixes) to stdin, read newline-separated selection from stdout. Landed as `048cdba`.
- [x] Selection parsing: strip the `[x] `/`[ ] ` prefix from each returned line; recognize sentinel rows; collision-guard sentinel names against real activity names at picker-open (rename to `__niri_activities_select_all__` / `__niri_activities_only_one__` if collision, log debug). Landed as `048cdba`.
- [x] Sentinel precedence (per §4.4): `« Only one… »` > `« Select all »` > literal selection. Landed as `048cdba`.
- [x] **Save semantics: full replacement, not diff.** Returned rows = saved membership set. Dispatch `Action::SetWorkspaceActivities { workspace: WorkspaceReferenceArg::Id(ws.id), activities }` with explicit workspace id to guard against focus drift between picker open and save. Landed as `048cdba`.
- [x] `« Only one… »` chained picker: spawn a follow-up `rofi -dmenu -p 'Only this activity:'` (no `-multi-select`, no sentinels) with the activity list; the picked activity becomes the sole membership. Cancellation of the chained picker → exit 0 silently, no IPC. Landed as `048cdba`.
- [x] Wire to `assign-workspace` subcommand: query `Request::Activities` + `Request::Workspaces` for current memberships + focused workspace → build pre-marked input → present picker → resolve sentinels per precedence → dispatch `Action::SetWorkspaceActivities`. Landed as `048cdba`.
- [x] Cancellation of the primary picker (rofi non-zero exit, empty stdout) → exit 0 silently, no IPC. Landed as `048cdba`.
- [x] Tests: `tests/rofi_shim.rs` integration tests via shim binary on `PATH` overriding `rofi` (three-`accept()` one-shot listener for the new three-IPC-round-trip dispatch flow); unit tests for prefix-parsing and sentinel-precedence pure functions. +24 tests total (+18 unit / +1 CLI integration / +4 rofi_shim integration / +1 picker_shim integration fixer-add). Landed as `048cdba`.
- [x] README updated: rofi listed as a runtime dep alongside fuzzel; install hint for Debian (`apt install rofi`); note that rofi 2.0+ is required for native Wayland-layer-shell; PoC-quality caveat documented. Landed as `048cdba`.
- [x] Stretch: handle stale snapshot — an external `niri-activities create` while the picker is open. v1 takes a snapshot at picker-open; if stale at save, the IPC error surfaces normally per §4.1. Document; defer reactive refresh to v2 (Appendix B). Landed as `67097d1` (rustdoc note on `assign_workspace::run` documenting snapshot-freshness contract; reactive refresh deferred to v2).

**Reviewed:** 2026-05-14 (`048cdba`, was `466c363` before the post-review amend; follow-up `8e4ec26` for the `cmd_switch` parallel `.context` bug surfaced by review). Phase 3.5b — nine non-stretch boxes (3.5b-1 through 3.5b-9) bundled into one amended commit plus one follow-up: module reshape `src/picker.rs` → `src/picker/{mod.rs, single_select.rs, multi_select.rs}` (re-exports preserve `crate::picker::pick_one` / `::ensure_available` / `::PickerOutcome` for `src/cli.rs` and `src/switch.rs` — zero call-site edits outside `picker/`); new `src/picker/multi_select.rs` with sentinel parsing, chained-picker control flow, two-arm `ensure_available` matching the fuzzel precedent, and `ROFI_MISSING_MESSAGE`; new `src/assign_workspace.rs` wiring `Request::Activities` + `Request::Workspaces` + `Action::SetWorkspaceActivities` (first production use of the latter); `cmd_assign_workspace` in `src/cli.rs` wired; new `tests/rofi_shim.rs` with three-`accept()` listener; +24 tests overall. Follow-up landed as `8e4ec26` (`cli: differentiate context strings on cmd_switch picker steps`). Reviewed across five aspects (code quality, silent-failure surface, comment accuracy, test coverage, type design). 21 review findings classified by fixer: 15 squashed into the amended primary, 1 follow-up (`8e4ec26`), 6 skipped (reviewer-marked optional or covered elsewhere), 2 parked to Appendix C, 0 escalations. **Finding worth surfacing (Q1 resolution — drop `« Select none »` sentinel):** the original §4.4 wording promised "saves the state as 'no activities'" but the compositor invariant in `niri-ipc/src/lib.rs:1097–1099` makes empty activity lists a wire-rejected state; the DD was wire-incorrect. v1 picker offers two sentinels (`« Select all »`, `« Only one… »`); Escape cancellation covers "back out without saving." Reversal cost is low — v2 picker overhaul (Appendix B) can revisit if users miss the gesture. **Finding worth surfacing (Q2 resolution — no-focused-workspace → exit 65):** the compositor's "exactly one focused workspace at any time" invariant is treated as a hard wire-contract: zero `is_focused: true` entries in a `Workspaces` reply surfaces `MalformedResponse(Server("no focused workspace"))` (exit 65). The `"no focused workspace"` literal is a CLI-internal synthetic message, not a niri emission — rustdoc on `assign_workspace::focused_workspace` makes this explicit so future grep audits of compositor wire-string match sites skip the location. **Finding worth surfacing (`SetWorkspaceActivities` workspace-arg discipline):** `Action::SetWorkspaceActivities` is dispatched with `WorkspaceReferenceArg::Id(ws.id)` rather than `None`, guarding against focus drift between picker-open and save — a user pressing Alt-Tab or an urgent-window popping during the picker session would otherwise silently target the wrong workspace. The rofi-shim wire-body capture test and the `MockClient` JSON-equality check at the unit boundary both pin this contract. **Finding worth surfacing (doc-rot when adding a sibling module):** introducing `src/picker/multi_select.rs` alongside `src/picker/single_select.rs` rotted ~7 bare "the picker" / "picker shim" mentions across `single_select.rs`, `multi_select.rs:79`, and `tests/picker_shim.rs`; all fixed in the amended primary. Meta-learning: when adding a sibling module, grep the existing module's docs for bare "the [module-name]" qualifiers and disambiguate before landing. **Finding worth surfacing (duplicate `.context(...)` strings collapse the stderr chain):** both `ensure_available()` and `assign_workspace::run()` initially carried identical `.context("running assign-workspace picker")`, collapsing the anyhow chain into an indistinguishable pair. Fixed in the primary: `"verifying assign-workspace picker availability"` vs. `"running assign-workspace picker"`. Same pattern existed pre-Phase-3.5b in `cmd_switch` (`src/cli.rs:104/106`); fixed by `8e4ec26`. Meta-learning: when two adjacent `?` sites in the same function share the same `.context(...)` string, differentiate them (setup vs. operation). **Finding worth surfacing (test count claim verifies):** implementer's `+18 unit / +1 CLI / +4 rofi_shim` reconciles exactly with the spec's enumerated budget; the spec's "approximately 128" prose was slack — actual delivery is 124, correct against the breakdown. Post-review fixes squashed into `048cdba`: 15 findings fixed (module-doc disambiguation, duplicate-context differentiation on `assign_workspace`, `WorkspaceReferenceArg::Id` explicit-id form, `« Select none »` sentinel removal, two-sentinel precedence rewrite, underscore-fallback list trimmed, `SentinelNames` struct field naming, `ROFI_MISSING_MESSAGE` scoping, rofi-shim wire-body capture test, unit tests for sentinel parsing edge cases, and further rustdoc tightening). DD also amended in this commit: §3 `assign-workspace` bullet updated; §4.4 bulk-actions rewritten to two sentinels; §4.4 sentinel-precedence rewritten to three rules; §4.4 magic-item-naming list trimmed; §4.4 "Focused-workspace contract" paragraph added; Phase 3.0 picker-contracts box updated; Phase 3.5b boxes 3.5b-1 through 3.5b-9 flipped to `[x]`; Appendix A extended; Appendix B "No-focused-workspace" entry added; Appendix C extended with two type-design-polish items and two post-landing notes. Same 124 tests green (103 unit + 10 CLI integration + 6 picker_shim integration + 4 rofi_shim integration; was 100; delta +24); `cargo clippy --all --all-targets` no new warnings (zero-warning baseline held). Proceed to Phase 3.6 (action subcommands) with the reviewed base.

### Phase 3.6 — Action subcommands

Group landings — most of these are 1–2 line wrappers around a single `Action` variant, plus a small bit of arg parsing. Group by shared scaffolding (the picker dance, the `<name>`-or-picker pattern). `assign-workspace` is **not** in this list — it landed in Phase 3.5b alongside its bespoke picker.

- [x] `switch-previous` / `toggle` (alias) — wraps `Action::SwitchActivityPrevious`. Landed as `67097d1`; toggle alias pinned to `SwitchActivityPrevious` (not `Switch`) by `87d05e0`.
- [x] `move-window <activity>` and `move-window` (picker variant) — composes `Action::MoveWindowToWorkspace` against the destination activity's trailing-empty workspace on the focused output (no new IPC variant; compositor DD §4.3 forbids `Action::MoveWindowToActivity` by design — workspace-as-atom). See §3 `move-window` for the resolution strategy and the non-current-activity fallback. The prior **Blocked** annotation was a wrong-premise rebound (Appendix C entry 1, now closed); architect spec settles the fallback shape and the picker UX (lift the `move-workspace` precedent). No compositor-loop or rev-bump prerequisite. Landed as `fa51a5e`.
- [x] `move-window-here` — single-stage picker pinned to the currently-active activity; keybind-ergonomic degenerate of the two-stage `move-window` flow. Picker presents workspaces in the active activity on the focused output; `« New workspace »` appended as the last row. Dispatches `Action::MoveWindowToWorkspace { reference: Id(_), window_id: None, focus: false }` against the selected or newly-created workspace. Landed as `fa51a5e`.
- [x] `move-workspace <activity>` and `move-workspace` (picker variant) — wraps `Action::MoveWorkspaceToActivity { workspace: None, activity: Name(_), focus: false }`. Landed as `67097d1`.
- [x] `create <name>` — wraps `Action::CreateActivity`. Name collision → exit 73 (EX_CANTCREAT). Landed as `f0b22e8`; open-coded match (not `send_expect_handled`) because the create error matrix (`"activity name already exists"` → `CantCreate`, `"activity name must not be empty"` → `Usage`) has no `"activity not found"` arm.
- [x] `remove <name>` — wraps `Action::RemoveActivity`. Unknown name → exit 66; removing a config-declared activity surfaces the compositor's error verbatim. Landed as `f0b22e8`; reuses `send_expect_handled`; config-declared refusals, last-activity refusals, and workspace-has-windows refusals pass through `MalformedResponse(Server)` (exit 65).
- [x] `save <name>` — non-IPC: edits user's `config.kdl` (appending `activity "name"`), then `Action::ReloadConfig`. Decide config-edit strategy: structured (`kdl` crate) vs. string-append heuristic. The structured path is safer (handles arbitrary existing config); the heuristic ships fast. Pick during this sub-phase based on `kdl` crate maturity. Uses the `kdl` crate's KDL-v1 parser to preserve user-config formatting; falls back to `CliError::ConfigEdit` on parse error. Landed as `0a5a5b8`.

**Reviewed:** 2026-05-15 (`67097d1`, `d72cb30`, `4e0dd56`, `87d05e0`). Phase 3.6 (boxes 1 and 3) and the Phase 3.5b stretch box across four commits: `67097d1` is the amended primary wiring `switch-previous` / `toggle` and `move-workspace`, extracting `ipc_helpers`, and landing the Phase 3.5b stretch rustdoc note; `d72cb30` pins `variant_name` exhaustiveness for all known `Response` variants; `4e0dd56` adds `run_picker WrongVariant` unit tests for both `switch.rs` and `move_workspace.rs`; `87d05e0` adds a request-shape pin for the `toggle` alias. Reviewed across five aspects (general code quality, silent-failure surface, comment accuracy, test coverage, type design). **Finding worth surfacing (`Fn` → `FnOnce` on picker closures):** `run_picker<F>` in both `src/switch.rs:98` and `src/move_workspace.rs:94` accepted `Fn` bounds in the pre-3.5 `switch.rs` implementation that was lifted as the template; both sites tightened to `FnOnce` in `67097d1`. Picker closures are one-shot — `FnOnce` encodes the correct call-count contract and prevents unintentional re-call patterns. Future picker-using subcommands should start with `FnOnce`. **Finding worth surfacing (`send_expect_handled` `debug_assert!` empty-name guard):** `ipc_helpers::send_expect_handled` accepts `activity_name: Option<&str>`; passing `Some("")` would silently produce `CliError::ActivityNotFound("")` — a typed carrier with a misleading empty name. A `debug_assert!(name.map_or(true, |n| !n.is_empty()))` guard at the function entry catches this in test builds. **Finding worth surfacing (`"workspace not found"` → `MalformedResponse(Server)`, not `ActivityNotFound`):** `move_workspace.rs` routes the compositor's `"workspace not found"` string to `MalformedResponse(Server)` (exit 65) rather than `ActivityNotFound` (exit 66). The string mentions a workspace, not an activity; routing through `ActivityNotFound` would misname the missing resource. A negative-space unit test pins this routing. **Finding worth surfacing (`variant_name` exhaustiveness pinned by `d72cb30`):** the `_` catch-all arm in `variant_name` would silently degrade to `"Response::<unknown>"` when `niri-ipc` adds a new variant; `d72cb30`'s `variant_name_covers_all_known_variants` test enumerates every known variant so upstream additions surface as a compile/test failure rather than a wrong-name in `WrongVariant::got` error messages. This closes the Appendix C forward-compat gap entry partially. Post-review fixes squashed into `67097d1`: `Fn` → `FnOnce` on `run_picker<F>` in `src/switch.rs:98` and `src/move_workspace.rs:94`; comment-rot in `move_workspace.rs` rustdoc corrected (over-broad invariant claims on `"workspace not found"` qualifier, `"workspace not in active activity"` reachability, and dangling cross-reference to a non-existent "differentiated-context-strings convention" in `cli.rs`); `debug_assert!` empty-name guard added to `ipc_helpers::send_expect_handled`. Follow-up landed as `d72cb30` (`ipc_helpers: pin variant_name exhaustiveness for all known Response variants`). Follow-up landed as `4e0dd56` (`switch, move_workspace: add run_picker WrongVariant unit tests`). Follow-up landed as `87d05e0` (`cli: pin toggle alias routes to SwitchActivityPrevious, not Switch`). Same 159 tests green (was 124; delta +35 — +30 from `67097d1`, +5 from `d72cb30` + `4e0dd56` + `87d05e0`). Proceed to Phase 3.6 (boxes 2, 4, 5, 6: `move-window`, `create`, `remove`, `save`) with the reviewed base.

**Reviewed:** 2026-05-15 (`f0b22e8`). Phase 3.6 boxes 4 (`create`) and 5 (`remove`) in one squashed commit: `src/create.rs` wires `Action::CreateActivity { name }` with an open-coded match routing `"activity name already exists"` → `CliError::CantCreate` (exit 73, first production constructor for that variant) and `"activity name must not be empty"` → `CliError::Usage` (exit 64); `src/remove.rs` wires `Action::RemoveActivity { activity: Name(name) }` reusing `send_expect_handled`, with config-declared-activity / last-activity / workspace-has-windows refusals passing verbatim through `MalformedResponse(Server)` (exit 65). Reviewed across five aspects (general code quality, silent-failure surface, comment accuracy, test coverage, type design). **Finding worth surfacing (`remove.rs` wire strings drifted from compositor `Display` impl):** four reviewers independently flagged that the initial `remove.rs` test wire strings were reconstructed from rustdoc prose rather than read from the compositor's `RemoveActivityError::Display` impl. Two wire-string substitutions landed in `f0b22e8` plus one new test for the `ExclusiveWorkspaceHasWindows` path. Module rustdoc on `remove.rs` was rewritten to reference the compositor's `Display` impl symbolically (`"<RemoveActivityError variant>::fmt"`) rather than quote example strings, making it drift-resistant. Future action-subcommands must read the compositor's error `Display` impl directly before writing a test wire string, not infer from rustdoc or prose. **Finding worth surfacing (`create.rs` open-coded match lacked negative-space coverage):** the initial `create.rs` open-coded match had no strict-equality negative-space tests — the match is `msg == "..."` but no test pinned that a suffix-drifted or capitalization-drifted string routes to `MalformedResponse(Server)` rather than `CantCreate`. Two tests added in `f0b22e8` (suffix-drift and capitalization-drift for `"activity name already exists"`), bringing `create.rs` to parity with the precedent from Phase 3.4 `switch.rs`. **Finding worth surfacing (`CliError::CantCreate` first production constructor):** the Phase 3.1 scribe note flagged `NotImplemented` as a transient variant expected to vanish as sub-phases land; `f0b22e8` gives `CantCreate` its first production constructor (previously it was live in `map_exit` but unreachable from any real code path). The companion `#[allow(dead_code)]` on the `CantCreate` variant in `src/error.rs` should be lifted now that a production constructor exists; remaining `CliError` variants under `#[allow(dead_code)]` should be audited in a Phase 3.6 or Phase 3.7 cleanup pass. Post-review fixes squashed into `f0b22e8`: two `remove.rs` wire-string substitutions (`src/remove.rs` test arms corrected against compositor `Display`); one new `remove.rs` test for `ExclusiveWorkspaceHasWindows`; two `create.rs` negative-space tests added (`src/create.rs` strict-equality pin for suffix-drift and capitalization-drift); inline comment on `let result: anyhow::Result<()>` E0282 inference annotation added in `src/create.rs`; symmetric "why we DO use `send_expect_handled`" rustdoc added to `src/remove.rs`. DD also amended in this commit: Phase 3.6 boxes 4 and 5 flipped to `[x]` with landing notes; box 2 `move-window` blocking annotation added; Appendix C "Typed `Reply::Err` variants" tally updated; Appendix A extended with `src/create.rs` and `src/remove.rs`. Same 177 tests green (was 159; delta +18 — +15 from the initial implementation as specified; +3 from fixer additions: 1 new `remove.rs` negative-space test + 2 new `create.rs` negative-space tests). Proceed to Phase 3.6 (box 6: `save`; box 2 `move-window` blocked pending compositor rev-bump) with the reviewed base.

**Reviewed:** 2026-05-15 (`0a5a5b8`, was `1342dee` → `8e225ad` → `0a5a5b8` across two post-review amends). Phase 3.6 box 6 (`save <name>`) in one squashed commit: `src/save.rs` parses the user's niri config as KDL v1 via the `kdl` crate, appends an `activity "<name>"` top-level node when no case-insensitive collision exists, atomically writes back (write-to-tmp + rename), then dispatches `Action::LoadConfigFile { path: None }`; already-declared collisions short-circuit before the filesystem is touched and exit 0 without an IPC call; `ConfigPathResolver` trait drives in-process tests without real env state. Reviewed across five aspects (general code quality, silent-failure surface, comment accuracy, test coverage, type design) — 22 actionable findings across the two amend cycles. **Finding worth surfacing (kdl-rs v6 KDL v2 bareword surprise):** kdl-rs v6 defaults to KDL v2 dialect; `KdlValue::String` Display picks bareword form for plain idents — `activity Work` rather than `activity "Work"` — which niri's KDL v1 parser would read as an identifier, not a string. The new `quoted_string_entry` helper pins the entry's `value_repr` to a quoted-string shape (`"Work"`) so the serialised form always round-trips through niri's parser. Any future config-edit subcommand that emits string-valued KDL nodes must call `quoted_string_entry` (or equivalent `set_value_repr`) rather than constructing a bare `KdlEntry::new`. **Finding worth surfacing (catch-all Transport arm needs reload-failure stderr breadcrumb):** cycle-1 review found that the `IpcError::Transport` catch-all arm in `dispatch_reload` (reached when the niri socket is unavailable after a successful config-file write) emitted no `eprintln!` breadcrumb before returning the error, while every other error arm did. Cycle-2 re-review confirmed all arms — including Transport — now emit the same `"niri-activities save: reload request failed: ..."` breadcrumb so stderr is never silent on reload failure regardless of IPC error variant. **Finding worth surfacing (malformed-config reject-loud path via `append_activity_if_absent`):** `append_activity_if_absent` rejects non-`KdlValue::String` first entries in existing `activity` nodes via an explicit check rather than silently treating them as non-colliding names. This means a user config with `activity 42` (integer node) surfaces a `CliError::ConfigEdit` error rather than appending a duplicate node alongside the malformed one — fail loud preserves the user's config state. **Finding worth surfacing (`Action::LoadConfigFile.path` is `Option<String>` at the pinned IPC rev, not `Option<PathBuf>`):** the spec said `Option<PathBuf>`; the pinned rev (`54aee6582cbfc11b4e69fa8a602cf2653e29df4a`) exposes `Option<String>`. Dispatching `None` as this sub-phase does is unaffected, but any future sub-phase wanting to dispatch `Some(_)` (targeted reload) must use `Option<String>`, not `Option<PathBuf>`. **Finding worth surfacing (`directories` dep pinned at v6, not v5):** the spec mentioned v5; `0a5a5b8` pins v6 to match the niri fork's own `directories` pin. No API difference for the `project_dirs` call path used here; recorded for the next rev-bump sub-phase that touches `Cargo.toml`. **Note on runtime deps:** `save` introduces no new runtime deps; `kdl` and `directories` are compile-time Cargo deps, not runtime binaries. The README runtime-deps stanza (Phase 3.7) remains unchanged by this sub-phase. Post-review fixes squashed into `0a5a5b8`: C1 — reload-failure stderr breadcrumb added on all error arms including the catch-all Transport branch (`src/save.rs`); C2 — non-`String` first entries in existing `activity` nodes in `append_activity_if_absent` now surface `ConfigEdit` rather than silently bypassing collision detection (`src/save.rs`); rustdoc citations of hard-coded niri source-tree paths dropped (rot-prevention; `src/save.rs`); tests added for `Reply::Err` server-error arm, KDL parse-back round-trip, `RealConfigPaths` env-variable coverage (`src/save.rs::tests`); `save_no_socket_exits_69` binary-boundary integration test added pinning that the fs-edit succeeds and the reload IPC step crosses the dead socket (`tests/cli.rs`). DD also amended in this commit: §3 `save` bullet extended with config-edit strategy; §4.1 exit-code table row 73 extended to include `ConfigEdit` as a co-carrier alongside `CantCreate`; §4.1 prose `CliError` listing extended with `ConfigEdit`; Appendix A extended with `src/save.rs`; Appendix C extended with four entries (two parked suggestions, two Phase 3.x escalation candidates). Same 193 tests green (was 177; delta +16 — +14 unit in `save.rs`, +1 in `error.rs`, +1 integration `save_no_socket_exits_69`); `cargo clippy --all --all-targets` 0 warnings (zero-warning baseline held). Proceed to Phase 3.7 (polish & v0.1.0; box 2 `move-window` still blocked on compositor rev-bump) with the reviewed base.

**Reviewed:** 2026-05-16 (`fa51a5e`, was `2962390` before the post-review fixer-squash amend). Phase 3.6 box 2 (`move-window` + `move-window-here`) — post-v0.1.0 landing; the box was previously annotated Blocked (wrong premise: `Action::MoveWindowToActivity` never existed by compositor DD §4.3 design), resolved by the architect spec in the same session. `fa51a5e` lands `src/move_window.rs` (~1265 source lines, 43 unit tests); wires `Cmd::MoveWindow` and new `Cmd::MoveWindowHere` in `src/cli.rs` + `src/main.rs`; and fires the 5th-call-site rule on `send_expect_activities` / `send_expect_workspaces`, extracting both into `src/ipc_helpers.rs` and migrating 4+1 / 2+1 existing sites. Reviewed across five aspects (general code quality, silent-failure surface, comment accuracy, test coverage, type design) — 25 findings total across five parallel reviewer threads, all squashed into `fa51a5e`; zero follow-up commits. **Finding worth surfacing (`Unknown` variant added to `Stage1Resolution` / `Stage2Resolution`):** both resolvers previously folded "picker returned a row not in items we passed" into `Cancelled`, conflating a picker-side contract violation with user gesture; the fix adds `Unknown(String)` as a fourth variant on both enums, routed to `MalformedResponse(Server("picker returned row not in items"))` (exit 65). Resolver functions returning plain enum values must never silently fold contract violations into user-gesture variants — this is the codified meta-rule for future picker-using subcommands. **Finding worth surfacing (`Stage2Resolution::NewWorkspace` defensive arm flipped to `unreachable!`):** the `NewWorkspace` branch in `dispatch_stage2` was a reachable-by-callers but unreachable-via-picker-UI arm carrying a `MalformedResponse` fallback; flipped to `unreachable!()` so a future regression fails loudly. A stretch type-state refactor making `NewWorkspace` un-constructible when `include_new_workspace=false` was escalated as architectural and parked in Appendix C. **Finding worth surfacing (named-arg eprintln message split):** when the named activity had workspaces on the focused output but none were trailing-empty, the error message incorrectly said "has no workspaces on the focused output"; split into two distinct cases ("no workspaces" vs. "no empty workspaces; create one or pick existing via picker"); the asymmetric error in `dispatch_stage2` ("trailing-empty resolution failed for non-active activity") was also corrected — it could fire for the *active* activity in a multi-output edge case. **Finding worth surfacing (`pub(crate)` over-exposure — second sub-phase):** all internal helpers and types in `move_window.rs` were `pub(crate)` despite no sibling-module callers; demoted to private. This is the second sub-phase where this pattern surfaced (compare Phase 3.6 boxes 1+3); meta-rule for cli-implementer specs: all helpers below the public entry points in a module are `fn` unless called from a sibling module. **Finding worth surfacing (`current_activity_id_and_name` helper extracted for `.expect()` panic elimination):** two `.expect("current_activity_id resolved against the snapshot we just queried")` calls (pre-amend `src/move_window.rs:220, 261`) were a defensible invariant but a panic surface; `current_activity_id_and_name` returns `Result<(u64, String), CliError>` and both call sites now route through synthetic-error discipline. **Finding worth surfacing (DD-reference `(Q2)` shorthand in code):** three code sites used `(Q2)` as a DD-shorthand inline reference — removed; the bare prose explains each case without the DD cross-reference. The commit-message hook guards `§`, `Box`, `Phase`, `Appendix`, `design.md` but not bare `Q[0-9]+`; consider extending the pattern. Post-review fixes squashed into `fa51a5e`: `Unknown` variant on `Stage1Resolution` and `Stage2Resolution` with `MalformedResponse(Server)` routing (`src/move_window.rs`); `Stage2Resolution::NewWorkspace` arm flipped from `MalformedResponse` fallback to `unreachable!()` (`src/move_window.rs`); named-arg eprintln messages split and `dispatch_stage2` asymmetric error corrected (`src/move_window.rs`); all `pub(crate)` internal helpers and types demoted to private (`src/move_window.rs`); `current_activity_id_and_name` helper extracted from two `.expect()` call sites (`src/move_window.rs`); `(Q2)` shorthand references removed from code (`src/move_window.rs`); `spawn_two_shot_listener_for_move_window` rustdoc corrected (said "Accepts one connection" — accepts two; referenced non-existent `$SHIM_STAGE_BEHAVIOUR` env var — actual: `$SHIM_STAGE1_RESPONSE` / `$SHIM_STAGE2_RESPONSE`; `args:<path>` shim behaviour undocumented — added; `tests/picker_shim.rs`); `"trailing-empty resolution failed..."` synthetic-string carrier added to `move_window.rs` rustdoc enumeration of carriers; module rustdoc updated to enumerate the three remaining carriers after the `NewWorkspace`-to-`unreachable!` flip; +11 unit / integration tests added by fixer (`src/move_window.rs`, `tests/cli.rs`, `tests/picker_shim.rs`). DD amended in this commit: §3 `move-window` rewritten (architect pass); §3 `move-window-here` subsection added; §4.4 "Two-stage single-select picker" subsection added; Phase 3.6 box 2 ticked `[x]`; Phase 3.6 `move-window-here` sibling box added and ticked `[x]`; Appendix C entry 1 (Fork-side `MoveWindowToActivity`) annotated won't-fix — superseded; Appendix A extended with `src/move_window.rs` and `src/ipc_helpers.rs` extraction note. **v0.1.1 tag disposition:** this landing closes the last open Phase 3.6 box but does not introduce a user-visible feature boundary requiring its own semver bump today; v0.1.1 is deferred until either the type-state refactor (Appendix C) or the `niri-ipc` path-dep → git+rev migration lands — whichever comes first. A `v0.1.1` placeholder box is added under Phase 3.7 to make the tag boundary explicit. Same 255 default + 6 ignored = 261 tests green (was 244+6=250 post-implementer, baseline was 204+6=210; delta +11 from fixer additions); `cargo clippy --all --all-targets` no new warnings (zero-warning baseline held). Proceed to Phase 3.7 v0.1.1 tag with the reviewed base.

### Phase 3.7 — Polish & v0.1.0

- [x] README install/usage docs (currently a stub) — usage examples for every subcommand. Landed as `23ec8ee`. README rewritten from 32-line stub to v0.1.0 release docs; documents eight implemented subcommands, two runtime picker deps (fuzzel + rofi), source-only build path, exit-code table with stderr-prefix disambiguation for shared codes, and v1 caveats.
- [x] Manual smoke test against a running niri (the `--ignored` test layer per §4.6) — author the e2e tests, document the manual run cadence. Landed as part of the Phase 3.7 box 2 commit. `tests/smoke.rs` adds six `#[ignore]`-gated e2e cases (list / list --json / create-remove / collision / switch-round-trip / switch-previous) asserting post-IPC side effects via a `niri msg --json` round-trip rather than exit codes alone. README gains a "Manual smoke test" section documenting the run cadence (`cargo test --test smoke -- --ignored --test-threads=1`), prerequisites, side-effect-recovery procedure, and the explicit out-of-scope list (`save`, picker-driven variants).
- [x] `cargo clippy --all --all-targets` clean against the baseline established in Phase 3.1. Verified zero-warning clippy baseline held against `23ec8ee` — no delta vs. prior reviewed base.
- [x] Tag `v0.1.0`. Landed as annotated tag `v0.1.0` on `cb8b573` (the Phase 3.7 box 2 reviewed scribe commit); tag type verified as `tag` (annotated, multi-line release-note body), not `commit` (lightweight).
- [ ] Tag `v0.1.1`. Deferred from Phase 3.6 box 2 landing (`fa51a5e`); see Phase 3.8 for the two prerequisites. **One of two now closed: Phase 3.8 box 1 lands the type-state refactor (`6b7de15`).** Remaining blocker: niri-ipc path-dep → git+rev migration. Mirrors the `v0.1.0` tagging discipline (annotated tag on the reviewed scribe commit).

**Reviewed:** 2026-05-16 (`23ec8ee`). Phase 3.7 box 1 (README rewrite) bundled with box 3 (clippy baseline verification) in one commit; boxes 2 (manual smoke test) and 4 (v0.1.0 tag) explicitly split into separate landing units. Reviewed across four review aspects (general code quality, silent-failure surface, comment accuracy, test coverage). **Finding worth surfacing (exit-code table inlines stderr prefix per row):** the README exit-code table disambiguates shared codes 69 and 73 by inlining the stderr prefix (`"niri socket unavailable:"` vs. `"picker unavailable:"` at 69; `"niri-activities save:"` context vs. `CantCreate` path at 73) in each row rather than relegating the disambiguation to a trailing paragraph — this matches the meta-learning from Phase 3.6 box 6 review that per-row inline anchors are more legible than footer notes. **Finding worth surfacing (`assign-workspace` "Only one…" fuzzel chain undocumented in original spec):** the spec's README placeholder did not mention that `assign-workspace` chains into a secondary fuzzel invocation when `« Only one… »` is selected; the rewrite adds a prose note under the `assign-workspace` usage section covering this path, which a first-time user would not discover from `--help` output alone. Post-review fixes: none needed in the commit itself. DD also amended in this commit: Phase 3.7 boxes 1 and 3 flipped to `[x]` with landing notes; Appendix A extended with `README.md` after-Phase-3.7-box-1 entry; Appendix C "README runtime-deps section pending" entry closed. Same 204 tests green (174 + 17 + 9 + 4); `cargo clippy --all --all-targets` no new warnings (zero-warning baseline held). Proceed to Phase 3.7 box 2 (manual smoke test) with the reviewed base.

**Reviewed:** 2026-05-16 (`9479824`, was `95f202c` before the post-review amend). Phase 3.7 box 2 (manual smoke test layer) in one squashed commit: `tests/smoke.rs` adds six `#[ignore]`-gated e2e cases exercising the live niri fork, `README.md` gains a "Manual smoke test" section. Reviewed across five aspects (general code quality, silent-failure surface, comment accuracy, test coverage, type design). **Finding worth surfacing (`RuntimeActivityGuard` backstop disciplined uniformly):** `smoke_create_then_remove` initially lacked the `RuntimeActivityGuard` that `smoke_create_collision_exits_73` used from the start; the squashed amend adds it so both create-touching tests are panic-cleanup-safe — any future smoke test that calls `niri-activities create` must follow this discipline to avoid leaking `__nact_smoke_*` activities on the live compositor. **Finding worth surfacing (`niri_msg()` distinguishes empty-stdout from invalid-JSON):** the helper now surfaces two distinct failure modes rather than collapsing both into a single parse error — empty stdout (socket replied but produced no output) vs. invalid JSON (output present but unparseable); this matters for post-IPC assertion diagnostics where the two failure modes have different root causes. **Finding worth surfacing (stderr collision-anchor tightened to `cannot create activity:`):** the initial implementation asserted the substring `"cannot create activity"` (without trailing colon); the amend tightens this to `"cannot create activity:"` matching the wire string at `src/error.rs:182`. Future smoke tests asserting compositor error strings must be read from the compositor's `Display` impl (same lesson as Phase 3.6 box 5 wire-string drift) — paraphrasing produces anchors that diverge silently when the compositor changes its formatting. **Finding worth surfacing (`.expect()` panic messages widened to include actual payload):** `.expect("…")` chains in `smoke_switch_round_trip` and `smoke_switch_previous` were rewritten as `unwrap_or_else(|| panic!("…; got: {activities}"))` so a test failure always shows the actual JSON rather than just a static assertion message — critical for diagnosing failures in a live-niri environment where the test runner is the only observer. **Finding worth surfacing (`unique_name()` clock fallback hardened):** `.unwrap_or(0)` on the clock read was a silent fallback to a fixed seed; replaced with `.expect("clock not set")` to catch the failure loudly rather than silently producing colliding test names that would corrupt isolation. Post-review fixes squashed into `9479824`: `RuntimeActivityGuard` added to `smoke_create_then_remove` (`tests/smoke.rs`); module docstring corrected to describe the guard discipline; README prerequisites tightened to call out the gajdusek-fork requirement (`niri msg activities` is fork-only); `.expect()` → `unwrap_or_else(|| panic!("…; got: {activities}"))` in `smoke_switch_round_trip` and `smoke_switch_previous`; `niri_msg()` empty-stdout vs. invalid-JSON failure modes distinguished; `unique_name()` `.unwrap_or(0)` → `.expect("clock not set")`; stderr collision-anchor tightened to `"cannot create activity:"`; speculative `"no previous activity"` wire-string comment replaced with accurate prose; unused `before_active` computation removed; `nact()` docstring corrected (describes `assert_cmd::Command` shape, not `cargo run`); `RuntimeActivityGuard::drop` docstring tightened on spawn-failure vs. non-zero-exit behaviour. Same 210 tests passing when invoked as `cargo test --test smoke -- --ignored --test-threads=1` (204 default-lane + 6 ignored); `cargo clippy --all --all-targets` no new warnings (zero-warning baseline held). Proceed to Phase 3.7 box 4 (`v0.1.0` tag) with the reviewed base.

**Reviewed:** 2026-05-16 (`cb8b573`). Phase 3.7 box 4 — `v0.1.0` annotated tag landing. Review was skipped: there are no implementer commits in this sub-phase; the deliverable is the tag itself, not a source diff. The architect's spec substituted an enumerated set of review-stop conditions that the implementer self-verified inline — all five pass: (1) annotated tag confirmed (`git cat-file -t v0.1.0` → `tag`, not `commit`); (2) tag object resolves to `cb8b57320088990e121a12911797e0d15875b5fb` — the Phase 3.7 box 2 reviewed scribe commit, which was HEAD at tagging time; (3) no source or `Cargo.toml` edits interleaved with the tag; (4) no push (tag is local; push happens only on explicit human request); (5) test and clippy baseline unchanged — 204 default + 6 ignored tests, zero clippy warnings. Phase 3.7 is now complete. `v0.1.0` is the explicit release boundary for the niri-activities CLI: eight subcommands implemented (`list`, `switch`, `switch-previous` / `toggle`, `move-workspace`, `assign-workspace`, `create`, `remove`, `save`); runtime picker deps are `fuzzel` (single-select) and `rofi` (multi-select for `assign-workspace`); source-only build due to the `niri-ipc` path-dep stopgap; `publish = false`. The lone unchecked Phase 3.6 box (`move-window`, line 368) remains explicitly blocked on `Action::MoveWindowToActivity` landing in the compositor fork — a compositor-loop sub-phase prerequisite — and is parked until that IPC variant is available. Post-review fixes: none (no commit to amend; tag is immutable). Same 204 default + 6 ignored tests green; `cargo clippy --all --all-targets` zero warnings (zero-warning baseline held). Phase 3.7 complete; next sub-phase subject to human direction (compositor rev-bump enabling `move-window`, or new CLI feature phase).

### Phase 3.8 — Type-design hardening for v0.1.1

- [x] `Stage2Resolution` type-state refactor — split into `Stage2ResolutionWithNew<'a>` (carries `NewWorkspace`; active-activity path) and `Stage2ResolutionLiteralOnly<'a>` (structurally omits `NewWorkspace`; non-active path). Landed as `6b7de15`.
- [x] `move_window.rs` sentinel-struct collapse — drop `SentinelNames` / `WorkspaceSentinelNames` single-field structs in `src/move_window.rs`; promote four constants (`UNICODE_CURRENT_ACTIVITY`, `FALLBACK_CURRENT_ACTIVITY`, `UNICODE_NEW_WORKSPACE`, `FALLBACK_NEW_WORKSPACE`) to module-level; thread `&'static str` (return type) and `&str` (parameter type) through `sentinel_names` / `workspace_sentinel_names` / `compose_stage1_items` / `compose_stage2_items_with_new` / `resolve_stage1` / `resolve_stage2_with_new`. `multi_select::SentinelNames` is the genuine two-field matched-pair sibling and stays untouched (its own Appendix C TD-1 polish entry remains parked). Closes the escalation surfaced at the Phase 3.6 box 2 (`fa51a5e`) review. Landed as `2e23edc` (was `29c0ee2` before fixer squash).
- [ ] `niri-ipc` path-dep → git+rev migration — switch `Cargo.toml` from `path = "../../niri/niri/gajdusek/niri-ipc"` to `git = "https://github.com/gajdusek/niri", rev = "<sha>"` once the `feature/activities` branch is pushed. See §4.2 stopgap note and Appendix C entry.
- [ ] Tag `v0.1.1` — annotated tag on the reviewed scribe commit after both prerequisites above are closed. Mirrors `v0.1.0` tagging discipline.

**Reviewed:** 2026-05-16 (`2e23edc`, was `29c0ee2` before the post-review fixer squash). Phase 3.8 box 1b — `SentinelNames` / `WorkspaceSentinelNames` sentinel-struct collapse in `src/move_window.rs`. `2e23edc` removes both single-field structs, promotes `UNICODE_CURRENT_ACTIVITY`, `FALLBACK_CURRENT_ACTIVITY`, `UNICODE_NEW_WORKSPACE`, `FALLBACK_NEW_WORKSPACE` to module-level `const`s, and threads `&'static str` / `&str` through six function signatures (`sentinel_names`, `workspace_sentinel_names`, `compose_stage1_items`, `compose_stage2_items_with_new`, `resolve_stage1`, `resolve_stage2_with_new`). Net -17 LOC (+79/-96); sentinel bytes are byte-identical pre/post — all eight unit tests that pin `"« Current activity »"` / `"__niri_activities_current_activity__"` and the workspace pair continue to assert the same bytes, now bound directly rather than via field access. Reviewed across two aspects (general code quality, comment accuracy); silent-failure surface, test coverage, and type-design skipped as structurally not applicable to a purely-removing refactor. Code-reviewer found zero actionable findings. Comment-analyzer reported 2 Important + 3 Suggestions; fixer applied all 5. **Finding worth surfacing (closes the type-design escalation from `fa51a5e` cleanly, option (a) chosen over (b)):** the Appendix C entry first surfaced the tension at `fa51a5e` review; the choice between (a) `&'static str` constants and (b) a `Sentinel<T>` wrapper was explicit at spec time. Option (b) rejected because it substitutes one too-thin abstraction for another, with generics ceremony on top. The genuine matched-pair counterpart in `src/picker/multi_select.rs` (`SentinelNames` with two distinct sentinels travelling atomically) is preserved and cited in the module-level rustdoc so a future reader arriving via grep understands why the shapes diverged. **Finding worth surfacing (section-banner rot, Important #1):** the banner at `src/move_window.rs` line 86 read "Sentinel pairs" post-refactor; the body two lines below explicitly says "single sentinel per stage." Same class as the lines 947/1012 finding from the Phase 3.6 box 2 move-window landing review — banner-rot from a prior shape. Renamed to "Stage sentinels." **Finding worth surfacing (cross-reference deduplication, Important #2):** the implementer landed two rustdoc cross-references to `multi_select::SentinelNames` (module-level block + function-level rustdoc on `sentinel_names`), leaving `workspace_sentinel_names` without a peer. Fixer dropped the function-level cross-ref and kept the module-level paragraph as the single anchor; symmetry between the two helpers restored. **Finding worth surfacing (helper rustdoc contract-strengthening, Suggestions #3/#4):** both `sentinel_names` and `workspace_sentinel_names` rustdoc previously described implementation mechanics ("falls back when…"). Fixer rewrote to state the contract: "Returns the stage-N sentinel guaranteed not to collide with any element of `activity_names`. Prefers the unicode form; substitutes the underscore fallback iff a collision would occur." Names what callers depend on, not internal mechanics. **Finding worth surfacing (constant rustdoc cites collision-check timing, Suggestion #5):** both `FALLBACK_*` constants now cite their selecting helper and state "per picker invocation against the live … set"; a reader landing on a constant via grep sees when the collision check runs without laddering through the module-level body. **Finding worth surfacing (binary-boundary invariance held):** `tests/picker_shim.rs` is byte-identical pre/post — confirms the refactor is genuinely internal, no public API surface change. Post-review fixes squashed into `2e23edc`: all 5 comment-analyzer findings applied — section banner renamed from "Sentinel pairs" to "Stage sentinels" (`src/move_window.rs:86`); duplicate function-level `multi_select::SentinelNames` cross-ref dropped from `sentinel_names` rustdoc; `sentinel_names` and `workspace_sentinel_names` rustdoc rewritten to state contract over implementation; `FALLBACK_CURRENT_ACTIVITY` and `FALLBACK_NEW_WORKSPACE` constant rustdoc extended with selecting-helper citation and collision-check timing note. DD also amended in this commit: Phase 3.8 box 1b ticked `[x]` with landing note; Appendix C sentinel-struct entry annotated closed; Appendix A `src/move_window.rs` entry updated to note type collapse and four module-level constants. Same 259 default + 6 ignored tests green. Proceed to Phase 3.8 box 2 (`niri-ipc` path-dep → git+rev migration) with the reviewed base.

**Reviewed:** 2026-05-16 (`6b7de15`, was `8f06b0c` before the post-review fixer-squash amend). Phase 3.8 box 1 — `Stage2Resolution` type-state refactor that was escalated as architectural during the Phase 3.6 box 2 move-window landing review. `6b7de15` splits `Stage2Resolution<'a>` → `Stage2ResolutionWithNew<'a>` + `Stage2ResolutionLiteralOnly<'a>` in `src/move_window.rs`; splits `compose_stage2_items` / `resolve_stage2` / `dispatch_stage2` into matching `_with_new` / `_literal_only` pairs; branches `run_picker` into a two-arm dispatch on `Stage1Resolution::Selected.is_active`; `run_here_picker` unconditionally takes the with-new form. Removes `current_activity_id_and_name` helper (dead-name-weight after `target_activity_name` parameter drop from `dispatch_stage2_with_new`; both call sites revert to `current_activity_id` directly). Adds fourth synthetic-string carrier (`"trailing-empty workspace expected for active activity"`) replacing the former `unreachable!()` arm; module rustdoc carrier-count updated. Net +4 default tests (255 → 259). Reviewed across five aspects (general code quality, silent-failure surface, comment accuracy, test coverage, type design) — type-design reviewer scored 17/20 with explicit "no fixes required"; four other reviewers found 8 actionable items; fixer squashed 7, skipped 1 (F1: literal-only zero-case stderr substring test — structural guarantees pinned by exit-0 + no-picker-spawn + IPC-queue-drain; adding `gag` dev-dep for one assertion is overkill; binary-boundary integration test would cross the invariance line; revisit if UX regression risk rises). **Finding worth surfacing (type-state refactor closes the escalated MAJOR finding cleanly):** the original `Stage2Resolution<'a>` allowed `NewWorkspace` regardless of `include_new_workspace`. `Stage2ResolutionLiteralOnly` structurally omits `NewWorkspace` — illegal state unrepresentable on the literal-only path. Type-design reviewer scored 4/5 encapsulation, 4/5 invariant expression, 5/5 usefulness, 4/5 enforcement. **Finding worth surfacing (residual call-site-disciplined invariant):** the `NewWorkspace ⇔ active-activity` mapping is enforced at `run_picker` match arms (active activity → with-new) rather than at the type level. Tightening would require a newtype `ActiveActivityId(u64)` constructible only via `current_activity_id`. Type-design reviewer's recommendation: "next sub-phase if pursued, not a fix to this landing." Added to Appendix C. **Finding worth surfacing (`current_activity_id_and_name` removed as dead-name-weight):** the helper was introduced at `fa51a5e` for both stages; this refactor's `target_activity_name` parameter drop left both call sites discarding the name field. Code-reviewer and comment-analyzer both surfaced this independently; fixer removed the helper and reverted call sites to `current_activity_id`. Meta-learning: future cli-implementer specs should default to minimal return types and widen to tuples only when a concrete call site consumes both fields. **Finding worth surfacing (module rustdoc carrier-count synced):** the original module rustdoc enumerated three CLI-internal synthetic-string carriers; the refactor adds the fourth (`"trailing-empty workspace expected for active activity"`). `dispatch_stage2_with_new`'s function rustdoc previously claimed "user lands on trailing-empty slot" for the empty-filtered case — actually returns the new synthetic error; corrected. The `**Synthetic-string discipline.**` per-site annotation is applied to the new carrier site, matching the pattern at `focused_workspace` / `focused_output_name` / `current_activity_id`. Both comment-analyzer and silent-failure-hunter independently flagged the missing per-site annotation via different audit paths; consider codifying it as an explicit checklist item in cli-implementer's synthetic-carrier addition rule. **Finding worth surfacing (test count +4 reconciles cleanly):** three new behavioral tests (literal-only sentinel-absence, literal-only zero-case, with-new invariant-breach carrier) plus the `Unknown` resolver split (+1 from one test becoming two — one per enum type). **Finding worth surfacing (binary-boundary invariance held):** `tests/picker_shim.rs` is byte-identical pre/post — confirms the refactor is genuinely internal. Two existing integration tests (`run_picker_stage2_new_workspace_sentinel_resolves_to_trailing_empty`, `run_here_picker_new_workspace_sentinel_resolves_to_trailing_empty`) required zero body edits — public API surface unchanged. Post-review fixes squashed into `6b7de15`: 7 findings fixed — stale module rustdoc carrier count and `dispatch_stage2_with_new` rustdoc description corrected (`src/move_window.rs`); `**Synthetic-string discipline.**` per-site annotation added to fourth carrier site (`src/move_window.rs`); `Unknown` resolver split into two tests, one per enum type (`src/move_window.rs`); three new behavioral tests added (literal-only sentinel-absence, literal-only zero-case, with-new invariant-breach carrier); stale DD §4.4 `Stage2Resolution` prose refreshed to name the two type-state forms; Appendix C `Stage2Resolution::NewWorkspace` entry annotated closed. DD also amended in this commit: §4.4 `Stage2Resolution` prose refreshed (two type-state forms named, `include_new_workspace` parameter and `unreachable!()` arm noted as gone); Phase 3.7 box 5 `v0.1.1` annotation updated with Phase 3.8 cross-reference; Phase 3.8 section opened with three boxes (box 1 ticked `[x]`); Appendix C `Stage2Resolution::NewWorkspace` entry annotated closed by `6b7de15`; new Appendix C entry added for `ActiveActivityId(u64)` newtype; Appendix A `src/move_window.rs` entry updated to reflect type-state split and helper removal. Same 259 default + 6 ignored tests green. Proceed to Phase 3.8 box 2 (`niri-ipc` path-dep → git+rev migration) with the reviewed base.

### Phase 3.9 — Shell completion subcommand

User-facing: `niri-activities completions <shell>` emits a completion script to stdout, mirroring niri's own `niri completions <shell>` subcommand. Fish output is augmented with dynamic activity-name completion appended after the `clap_complete` base — tab-completing the positional `name` argument of `switch`, `move-window`, `move-workspace`, `remove`, `save`, and `assign-workspace` shells back into `niri-activities list --format=name` for live candidates. `create <name>` is deliberately absent from the dynamic set (the argument is a new name). Bash, zsh, elvish, and PowerShell receive the static `clap_complete` base only; dynamic variants for those shells are out of scope until there is concrete demand (different scripting per shell; fish is the primary target).

- [x] Cargo: add `clap_complete = "4.6"` (matches niri's 4.6.2 minor; loose constraint follows the existing convention for clap). Landed as `28658d8`.
- [x] `cli.rs`: `Cmd::Completions { shell: clap_complete::Shell }` variant with `#[arg(value_enum)]`, routed to `cmd_completions` → `completions::run(shell)` in `dispatch`. `tests/cli.rs` `help_lists_all_subcommands` extended with `"completions"`. Landed as `28658d8`.
- [x] `src/completions.rs` new module: `run(shell)` writes the `clap_complete` base via `clap_complete::generate(shell, &mut Cli::command(), "niri-activities", &mut out)`; fish branch appends a `# Dynamic activity-name completion.` header + 6 `complete -c niri-activities -n "__fish_seen_subcommand_from <verb>" -f -a "(niri-activities list --format=name 2>/dev/null)"` lines for the 6 dynamic verbs. Verb list lives in a `const FISH_DYNAMIC_VERBS: [&str; 6]` so a CLI surface change surfaces as a const edit; the candidate source command lives in `const FISH_NAMES_CMD`. `2>/dev/null` swallows the IPC error path so a stopped compositor yields zero candidates silently. Landed as `28658d8`.
- [x] Tests: 3 unit tests in `src/completions.rs` (`fish_dynamic_emits_one_line_per_verb`, `fish_dynamic_does_not_emit_line_for_create`, `fish_dynamic_uses_list_format_name_for_candidates`) pin the augmentation shape directly; 3 binary-boundary integration tests in `tests/cli.rs` (`completions_fish_emits_clap_complete_base_and_dynamic_lines`, `completions_bash_emits_non_empty_output`, `completions_unknown_shell_exits_64`) pin the subcommand wiring. Test delta +6 (259 → 265 default; 6 ignored unchanged). Landed as `28658d8`.

Landing notes (no `Reviewed:` annotation — sub-phase landed via the direct-implementation route per route-2 of the original plan; small scope, no `/pr-review-toolkit:review-pr` pass invoked). Cargo.lock catch-up for the fork's `niri-ipc` 25.11.0 → 26.4.0 bump landed in a sibling commit `3b1b798` immediately preceding `28658d8`; isolated so the lockfile delta and the feature commit each tell a single story. The completion file gets installed via chezmoi's `run_onchange_install-packages.sh.tmpl`, alongside the existing `niri` completion generation block.

**Follow-up fix (`d16a08d`) — position-aware fish conditions.** The original lines used `__fish_seen_subcommand_from <verb>`, which fires anywhere `<verb>` has appeared in the command line, including after the user has already supplied a positional name. The result: `niri-activities switch Foo <TAB>` continued offering activity names for a non-existent second positional; `help switch <TAB>` likewise. The fix splits `FISH_DYNAMIC_VERBS` into `FISH_SINGLE_ARG_VERBS` and `FISH_VARIADIC_VERBS`, and emits a new `__niri_activities_no_positional_yet` helper that counts non-flag tokens after the subcommand via `commandline -opc`. Single-arg verbs combine `__fish_niri_activities_using_subcommand <verb>` (clap_complete's own subcommand-context helper, stricter than `__fish_seen_subcommand_from`) with the new no-positional-yet guard; `assign-workspace` uses just the using-subcommand check because it is variadic. Test delta from this fix is +3 (`fish_dynamic_emits_using_subcommand_line_for_every_dynamic_verb`, `fish_dynamic_guards_single_arg_verbs_with_no_positional_yet`, `fish_dynamic_does_not_guard_variadic_verbs_with_no_positional_yet`, `fish_dynamic_defines_no_positional_yet_helper`, with the three pre-existing tests rewritten against the new substring shape). 268 default + 6 ignored tests green; clippy clean. **Finding worth surfacing (`__fish_seen_subcommand_from` is a foot-gun for positional-arg completions):** the fish docs gloss over its loose semantics ("any token in the command line matches"); reaching for it as the natural way to scope dynamic completions to a subcommand is exactly how this bug landed. The replacement pattern — clap_complete's `using_subcommand` + a custom `commandline -opc` token-count helper — is the right reflex for future verbs.

**Out of scope for this sub-phase (potential follow-ups):**

- Dynamic completion for bash/zsh (different scripting per shell; defer until demand surfaces).
- Workspace-name completion for `move-window-here` (would need a per-activity workspace enumeration; current picker UX already covers this).
- `niri-activities list --format=name` performance: the helper makes three IPC round-trips even when only the activity list is needed (per `src/list.rs:13`). At the current scale (<10 activities) the per-tab latency is negligible. If it becomes load-bearing, add a `--names-only` fast path that skips the workspace and window calls.

---

## Appendix A: Source code map (one-liner per file)

Populated as files land. Initial state: `src/main.rs` is the stub from the bootstrap commit (`92e26ef`).

**After Phase 3.1 (`413b49d`, `e7d6743`):**
- `src/main.rs` — entry point; clap `try_parse()` + manual clap-error routing (help/version → exit 0, parse errors → exit 64) + `map_exit` dispatch; prints full anyhow chain via `{:#}`.
- `src/cli.rs` — clap-derive `Cli` + `Cmd` enum with all nine subcommands; `dispatch()` routes to per-subcommand stub fns returning `CliError::NotImplemented` (exit 70).
- `src/error.rs` — `CliError` six-variant typed enum (Usage 64, MalformedResponse 65, ActivityNotFound 66, SocketUnavailable 69, NotImplemented 70, CantCreate 73) with rustdoc trigger conditions; `map_exit()` downcasts `anyhow::Error` via `downcast_ref`; un-typed errors fall through to exit 1.
- `tests/cli.rs` — `assert_cmd` integration smoke tests pinning the CLI surface: `--version`, `--help`, unknown-subcommand → exit 64, switch stub → exit 70.

**After Phase 3.2 (`38c2f18`):**
- `src/ipc.rs` — IPC trait `NiriClient`, `SocketClient` (one-shot `$NIRI_SOCKET` round-trip, open-coded to preserve `Decode`-vs-`Transport` distinction), `MockClient` (test fixture with `Drop`-enforced queue-consumption check), `IpcError` three-variant enum with rustdoc table, `IpcError` → `CliError` mapping via `From`; thread-local `make_client()` factory with `install_mock` RAII guard.

**After Phase 3.3 (`8fb13cf`):**
- `src/list.rs` — subcommand body for `list`; three render paths (plain default, `--json` envelope, `--format=<spec>` CSV); client-side join of Activities × Workspaces × Windows in fixed IPC order; first production constructor for `MalformedResponseSource::WrongVariant`.
- `src/error.rs` — gains `CliError::OutputPipeClosed` (typed sentinel for stdout-write BrokenPipe; emitted at `list` write sites, suppressed to exit 0 by `main.rs` before `map_exit`).
- `src/main.rs` — gains `is_stdout_pipe_closed` predicate + `OutputPipeClosed` → `ExitCode::SUCCESS` short-circuit ahead of `map_exit`; `list` arm in `dispatch()` now routes to `src/list.rs::run()`.
- `src/ipc.rs` — `#[allow(dead_code)]` markers removed from `NiriClient`, `IpcError`, and `make_client` (now consumed by production `list` call sites); test-only `MockClient::remaining_count()` accessor added.

**After Phase 3.4 (`8ae7384`):**
- `src/switch.rs` — `run(name: String, client)` dispatching `Action::SwitchActivity { activity: Name(name) }`; routes `"activity not found"` wire string to `CliError::ActivityNotFound` (exit 66), any other `Reply::Err` payload to `MalformedResponse(Server)` (exit 65), `Response::Handled` to `Ok(())`.

**After Phase 3.5 (`b11e7eb`, `5fb02c7`):**
- `src/picker.rs` — `pick_one(prompt, items, cmd_factory) -> Result<PickerOutcome, CliError>`: spawns `fuzzel --dmenu --prompt <prompt>`, writes items to stdin, reads stdout; `PickerOutcome::{Selected(String), Cancelled}`. `ensure_available()` and `which_in_path()` for `$PATH`-walk availability check (empty-component guard). `classify_output` maps non-zero exit + empty stdout to `Cancelled`; defensive `log::warn!` for non-zero + non-empty. `PICKER_MISSING_MESSAGE` constant (pub(crate)). Fuzzel stderr captured and re-emitted with `fuzzel:` prefix.
- `src/switch.rs` — gains `names_focused_first` (reorders activity list to surface focused activity) and no-arg dispatch path: `cmd_switch(None)` calls `ensure_available()` + fetches activity list via `Request::Activities` + calls `pick_one` + dispatches selected name via `run`.
- `src/cli.rs` — no-arg `switch` now routes through the picker path; `Cmd::Switch { name: Option<String> }`.
- `src/main.rs` — minor: `switch` arm in `dispatch()` now passes `Option<String>`.
- `tests/picker_shim.rs` — `assert_cmd` integration tests using a per-test tempdir `#!/bin/sh` shim named `fuzzel`, env-clear + scoped `$PATH`, and a one-shot `UnixListener` answering `Request::Activities`. Covers cancel → exit 0, select → dispatch, missing-from-PATH. `5fb02c7` adds `fuzzel_stdin_payload_reaches_shim` and `fuzzel_prompt_arg_is_switch_prompt` (sidecar-capture pattern: shim writes stdin/args to a tempfile path passed via env var; test reads and asserts).

**After Phase 3.5.5 (`5a50ef0`):**
- `src/error.rs` — gains `CliError::PickerUnavailable(io::Error)` (exit 69, Display `"picker unavailable: {io}"`; `Error::source` returns inner `io::Error`); `picker_unavailable_is_69` unit test added.
- `src/picker.rs` — `is_executable_file` widened to `Result<bool, io::Error>` (`Ok(true)` for executable regular file; `Ok(false)` for `NotFound` only; `Err(io)` for any other kind). `which_in_path` short-circuits on first non-`NotFound` error. `ensure_available` two-arm mapping: `NotFound` → synthetic `PICKER_MISSING_MESSAGE` wrapped in `PickerUnavailable`, non-`NotFound` → preserved `io::Error` in `PickerUnavailable`. All five picker call sites now route to `PickerUnavailable` (previously `SocketUnavailable`). `PICKER_MISSING_MESSAGE` reframed to `"fuzzel: not on $PATH (required for single-select picker)"`. Docstrings on `PICKER_MISSING_MESSAGE` and `ensure_available` tightened. **Renamed to `src/picker/single_select.rs` in Phase 3.5b.**
- `src/switch.rs` — `run_picker`'s empty-activities branch emits `"niri-activities: no activities configured; nothing to switch to"` to stderr before returning `Ok(())`.
- `tests/picker_shim.rs` — `run_picker_empty_activities_warns_and_exits_zero` integration test added: sentinel-file shim strategy (shim writes `: > "$SHIM_INVOKED"` on entry; test asserts sentinel absent) closes the shim-invocation observability gap on the zero-case path; `assert_cmd::Command::stderr(predicate)` asserts the stderr message at the process boundary. **Gains +1 fixer-added test in Phase 3.5b.**

**After Phase 3.5b (`048cdba`, `8e4ec26`):**
- `src/picker/mod.rs` — re-exports from `single_select` (preserving `crate::picker::pick_one` / `::ensure_available` / `::PickerOutcome` surface for `src/cli.rs` and `src/switch.rs` — zero call-site edits outside `picker/`) and from the new `multi_select` sibling.
- `src/picker/single_select.rs` — renamed from `src/picker.rs` (Phase 3.5.5 content unchanged in this sub-phase).
- `src/picker/multi_select.rs` — rofi multi-select adapter: `which rofi` availability detection, spawn `rofi -dmenu -multi-select`, sentinel-row injection and `[x] `/`[ ] ` pre-marker prefix rendering, sentinel-precedence resolution (`« Only one… »` > `« Select all »` > literal), chained single-select picker for `« Only one… »`, `SentinelNames` struct (two `String` fields for matched-pair unicode/underscore fallback), `ROFI_MISSING_MESSAGE`, `ensure_available` two-arm (NotFound → synthetic message, non-NotFound → preserved io::Error).
- `src/assign_workspace.rs` — `assign-workspace` subcommand body: `Request::Activities` + `Request::Workspaces` → focused-workspace extraction (`focused_workspace` fn with CLI-internal `"no focused workspace"` synthetic error; rustdoc marks this as not a niri wire string) → `workspace_activity_names` (`HashSet<String>` membership set) → multi-select picker → `Action::SetWorkspaceActivities { workspace: WorkspaceReferenceArg::Id(ws.id), activities }`. First production use of `SetWorkspaceActivities`.
- `src/cli.rs` — `cmd_assign_workspace` now wired (was `NotImplemented` stub); `8e4ec26` additionally differentiates the two `.context(...)` strings on `cmd_switch`'s `ensure_available` and `pick_one` call sites (`"verifying switch picker availability"` vs. `"running switch picker"`).
- `tests/cli.rs` — gains `assign_workspace_no_socket_exits_69` binary-boundary integration test.
- `tests/rofi_shim.rs` — 4 integration tests using a per-test tempdir `#!/bin/sh` shim named `rofi`, env-clear + scoped `$PATH`, and a three-`accept()` one-shot `UnixListener` for the three-IPC-round-trip dispatch flow (`Request::Activities`, `Request::Workspaces`, `Action::SetWorkspaceActivities`). Covers: literal selection wire-round-trip, `« Select all »` sentinel dispatch, `« Only one… »` chained-picker happy path, rofi-shim wire-body capture (JSON-equality at the process boundary).

**After Phase 3.6 boxes 1, 3 + Phase 3.5b stretch (`67097d1`, `d72cb30`, `4e0dd56`, `87d05e0`):**
- `src/switch_previous.rs` — `run(client)` dispatching `Action::SwitchActivityPrevious {}`; no name arg, no picker. Routes compositor's `"activity not found"` wire string defensively to `MalformedResponse(Server)` (no name in scope to attach to `ActivityNotFound`).
- `src/move_workspace.rs` — `run(name: String, client)` and `run_picker(client)` dispatching `Action::MoveWorkspaceToActivity { workspace: None, activity: Name(_), focus: false }`. Named + picker variants. Routes `"workspace not found"` to `MalformedResponse(Server)` (focused-workspace invariant breach, not an activity-not-found condition); routes `"activity not found"` to `ActivityNotFound`; `"workspace not in active activity"` falls through defensively to `MalformedResponse(Server)` (unreachable on `workspace: None` in practice).
- `src/ipc_helpers.rs` — shared `send_expect_handled(client, req, activity_name: Option<&str>)` (with `debug_assert!` empty-name guard); `variant_name(r: &Response) -> &'static str` (lifted from three previously-duplicated copies in `switch.rs` / `list.rs` / `assign_workspace.rs`); `names_focused_first` (lifted from `switch.rs`). `variant_name` covered by exhaustiveness test in `d72cb30`.
- `src/switch.rs` — imports `variant_name` and `names_focused_first` from `ipc_helpers` since this sub-phase; local copies removed.
- `src/list.rs` — imports `variant_name` from `ipc_helpers` since this sub-phase; local copy removed.
- `src/assign_workspace.rs` — gains rustdoc note on `run` documenting snapshot-freshness contract (reactive refresh deferred to v2). Local `send_expect_handled` retained (equivalent to `ipc_helpers::send_expect_handled(_, _, None)` — no `"activity not found"` special-case routing); `variant_name` migrated to `ipc_helpers`.
- `tests/cli.rs` — gains 4 exit-69 binary-boundary integration tests (switch-previous, toggle alias, move-workspace named, move-workspace no-arg).
- `tests/picker_shim.rs` — gains 3 tests (cancel, select, prompt-arg shim trio for the move-workspace picker variant).

**After Phase 3.6 boxes 4, 5 (`f0b22e8`):**
- `src/create.rs` — `run(name: String, client)` dispatching `Action::CreateActivity { name }`; open-coded match (not `send_expect_handled`) routing `"activity name already exists"` → `CliError::CantCreate` (exit 73, first production constructor), `"activity name must not be empty"` → `CliError::Usage` (exit 64), any other `Reply::Err` → `MalformedResponse(Server)` (exit 65). Seven unit tests including strict-equality negative-space tests for both match arms. Inline comment documents the E0282 `let result: anyhow::Result<()>` inference annotation.
- `src/remove.rs` — `run(name: String, client)` dispatching `Action::RemoveActivity { activity: Name(name) }`; reuses `send_expect_handled` for `"activity not found"` → `ActivityNotFound` (exit 66); all other compositor refusals (config-declared, last-activity, workspace-has-windows) pass verbatim through `MalformedResponse(Server)` (exit 65). Module rustdoc references the compositor's `Display` impl symbolically rather than quoting example strings (drift-resistant); rustdoc explains why `send_expect_handled` is used here (unlike `create`). Six unit tests including the `ExclusiveWorkspaceHasWindows` path.
- `tests/cli.rs` — gains 2 binary-boundary integration tests (`create_no_socket_exits_69`, `remove_no_socket_exits_69`).

**After Phase 3.6 box 6 (`0a5a5b8`):**
- `src/save.rs` — `save <name>` subcommand: `ConfigPathResolver` trait (two impls — `RealConfigPaths` reading `$NIRI_CONFIG` / `ProjectDirs::from("", "", "niri")`, and a test stub); `append_activity_if_absent` KDL-structured edit (parses user config via `kdl` crate, case-insensitive collision check, rejects non-`KdlValue::String` first entries, appends `activity "<name>"` node via `quoted_string_entry` helper pinning KDL v1 quoted-string `value_repr`); atomic write (write-to-tmp + rename); `dispatch_reload` dispatching `Action::LoadConfigFile { path: None }` with stderr breadcrumb on every error arm including the catch-all Transport branch. Fourteen unit tests (collision, round-trip, `Reply::Err` server-error arm, `RealConfigPaths` env-var coverage, malformed-config reject-loud path).
- `src/error.rs` — gains `CliError::ConfigEdit(io::Error)` (exit 73, co-carrier with `CantCreate`; used for KDL parse errors, atomic write failures, and missing home dir — all encoded as `io::Error::new(InvalidData, …)` / `io::Error::new(NotFound, …)` where no native `io::Error` is available).
- `tests/cli.rs` — gains `save_no_socket_exits_69` binary-boundary integration test pinning that the fs-edit completes and the reload IPC step crosses a dead socket (not exit 70 / `NotImplemented`, not exit 73 / fs-skipped).

**After Phase 3.7 box 1 (`23ec8ee`):**
- `README.md` — v0.1.0 release docs (was 32-line bootstrap stub); covers install, all eight implemented subcommands, runtime picker deps (fuzzel for single-select, rofi 2.0+ for `assign-workspace`), exit-code table with stderr-prefix anchors for shared codes 69 and 73, and v1 caveats (picker UX, `move-window` gap, source-only install, dotfiles-manager warning on `save`).

**After Phase 3.7 box 2:**
- `tests/smoke.rs` — `#[ignore]`-gated e2e smoke layer against a live niri. Six tests: `smoke_list_succeeds` (plain `list` returns non-empty stdout), `smoke_list_json_parses` (envelope `schema_version == 1` with an `activities` array), `smoke_create_then_remove` (create + remove round-trip verified via `niri msg --json activities`), `smoke_create_collision_exits_73` (second create at the same name exits 73 with stderr anchor `cannot create activity:`), `smoke_switch_round_trip` (focus moves via `Action::SwitchActivity`, restored after assertion), `smoke_switch_previous` (either focus changes on exit 0 or a niri-activities-prefixed stderr breadcrumb appears on non-zero exit). `require_live_niri()` precondition helper short-circuits with a `smoke: SKIP` breadcrumb when `$NIRI_SOCKET` is unset or unreachable. `niri_msg(socket, args)` shells out to `niri msg --json …` and decodes to `serde_json::Value`. `RuntimeActivityGuard` is a `Drop`-based best-effort cleanup handle (no `.unwrap()` in `Drop`; logs to stderr on failure). All runtime activities are named with the `__nact_smoke_<test>_<pid>_<nanos>` prefix for isolation and operator-side recovery. `save` and picker-driven subcommands are deliberately out of scope (would mutate `config.kdl` or require interactive pickers). Default `cargo test --all` count unchanged at 204; `cargo test --test smoke -- --ignored --test-threads=1` adds 6 more for 210 total.
- `README.md` — gains a "Manual smoke test" section documenting the run cadence (`cargo test --test smoke -- --ignored --test-threads=1` with `--test-threads=1` mandatory to avoid races), prerequisites (`$NIRI_SOCKET` set, `niri` on `$PATH`), side-effect recovery procedure (`niri-activities list | grep __nact_smoke`), and the explicit out-of-scope subcommands.

**After Phase 3.6 box 2 post-v0.1.0 (`fa51a5e`):**
- `src/move_window.rs` — `move-window` and `move-window-here` subcommand bodies; `Stage1Resolution` / `Stage2ResolutionWithNew` / `Stage2ResolutionLiteralOnly` typed enums with `Unknown(String)` variants for picker-contract violations (type-state split landed as `6b7de15`; the former `Stage2Resolution<'a>` and `bool include_new_workspace` parameter are gone); `run_named(name, client)` (non-interactive cross-activity dispatch via `send_expect_workspaces` + `send_expect_activities` + `Action::MoveWindowToWorkspace`); `run_picker(client)` (two-stage fuzzel flow; Stage 1 activity picker with `« Current activity »` sentinel; Stage 2 dispatches `compose_stage2_items_with_new` / `resolve_stage2_with_new` / `dispatch_stage2_with_new` for the active path and `_literal_only` variants for the non-active path); `run_here(client)` (single-stage workspace picker unconditionally using the with-new form); `dispatch_stage1` internal routing fn; `current_activity_id` call sites replacing the former `current_activity_id_and_name` helper (removed at `6b7de15` as dead-name-weight). Four CLI-internal synthetic-string carriers enumerated in module rustdoc (fourth carrier: `"trailing-empty workspace expected for active activity"` replacing the former `unreachable!()`). 259 default tests (net +4 from `6b7de15`). **After Phase 3.8 box 1b (`2e23edc`):** `SentinelNames` and `WorkspaceSentinelNames` single-field structs removed; four module-level `const`s added (`UNICODE_CURRENT_ACTIVITY`, `FALLBACK_CURRENT_ACTIVITY`, `UNICODE_NEW_WORKSPACE`, `FALLBACK_NEW_WORKSPACE`); six function signatures simplified to `&'static str` return type and `&str` parameter (sentinel_names, workspace_sentinel_names, compose_stage1_items, compose_stage2_items_with_new, resolve_stage1, resolve_stage2_with_new); duplicate `multi_select::SentinelNames` cross-reference collapsed to single module-level anchor. Net -17 LOC; test count unchanged at 259.
- `src/ipc_helpers.rs` — updated: `send_expect_activities` lifted from four prior duplicated sites (`switch.rs`, `move_workspace.rs`, `list.rs`, `assign_workspace.rs`) on the 5th-call-site rule fire; `send_expect_workspaces` lifted from two+one sites (`list.rs`, `assign_workspace.rs`, and the new `move_window.rs`). Both helpers are now the canonical form for their respective request types.
- `src/cli.rs` — `Cmd::MoveWindowHere` variant added; `cmd_move_window_here` entry point wired.
- `src/main.rs` — `MoveWindowHere` arm wired in `dispatch()`.
- `tests/picker_shim.rs` — gains two-shot listener helper `spawn_two_shot_listener_for_move_window` for the Stage 1 + Stage 2 fuzzel pipe flow; +11 tests total (move-window named dispatch, two-stage cancel paths, Stage 1 sentinel resolution, Stage 2 `« New workspace »` path, `move-window-here` picker dispatch, `move-window-here` cancel).

**After Phase 3.9 (`28658d8`):**
- `src/completions.rs` — `completions` subcommand: `run(shell)` calls `clap_complete::generate(shell, &mut Cli::command(), "niri-activities", &mut out)`; fish branch additionally invokes `emit_fish_dynamic` which appends 6 dynamic activity-name completion lines after the `clap_complete` base. Two module-level consts pin the contract: `FISH_DYNAMIC_VERBS: [&str; 6]` (the six verbs whose first positional arg is an existing activity name — `switch`, `move-window`, `move-workspace`, `remove`, `save`, `assign-workspace`; `create` deliberately absent) and `FISH_NAMES_CMD: &str` (the live-candidate source command, with `2>/dev/null` to silence the IPC error path). Three unit tests pin the augmentation shape directly (one-line-per-verb, no-line-for-create, list-format-name-source).
- `src/cli.rs` — gains `Cmd::Completions { shell: clap_complete::Shell }` variant (with `#[arg(value_enum)]`) and `cmd_completions` shim routing to `completions::run`.
- `src/main.rs` — `mod completions;` declaration added.
- `tests/cli.rs` — gains 3 binary-boundary integration tests pinning the subcommand wiring (`completions_fish_emits_clap_complete_base_and_dynamic_lines`, `completions_bash_emits_non_empty_output`, `completions_unknown_shell_exits_64`); `help_lists_all_subcommands` extended with `"completions"`. 265 default + 6 ignored tests green; zero clippy warnings.
- `Cargo.toml` — `clap_complete = "4.6"` added under `[dependencies]`.

## Appendix B: Open questions parked for v2

### Picker UX overhaul (whole subsection — track this together)

The v1 picker integration is PoC-quality (§1 caveat). Once the binary is functional end-to-end, evaluate whether the seams below collectively warrant a redesign or whether targeted fixes suffice. Decision deferred to "after the binary actually runs" per the design author's call.

- **rofi multi-row pre-check (upstream tracking).** [PR #1809 — Pre-check boxes on multi-select](https://github.com/davatorium/rofi/pull/1809) by KSXGitHub against `davatorium/rofi`, references issue [#1806](https://github.com/davatorium/rofi/issues/1806). Maintained by lbonn (Wayland-fork merger); open and unmerged as of 2026-04 with active history (force-pushed November 2024). **Watch this PR.** If it merges, the v1 `[x] `/`[ ] ` prefix workaround in §4.4 can be retired in favor of native pre-check, and the "PoC-quality" caveat shrinks meaningfully. If it stalls indefinitely, escalate by either patching `rofi` locally for development, switching launchers, or accepting the workaround.
- **Picker tool re-evaluation.** rofi may turn out to be the wrong fit for reasons not yet visible. Candidates already canvassed in §4.4 ("Why rofi over the alternatives canvassed" table): yad `--list --checklist` (Plan B if rofi unavailable on target distros — GTK3 + Wayland + filter), zenity (no live filter), TUI options (gum — terminal subprocess overhead), custom gtk4-layer-shell popup (rejected for Rust GUI dep weight). Re-litigate if real usage surfaces blocker-class issues with rofi.
- **Generalized single-select replacement for fuzzel** on `switch` / `move-window` / `move-workspace`. The two-tier split (fuzzel vs. rofi) is by selection cardinality. If users find fuzzel limiting (previews, richer rendering, activity icons), `rofi -dmenu` (single-select mode) is a near-drop-in replacement. Behind real UX evidence, not speculative.
- **Reactive refresh of the multi-select picker** on external activity changes during a picker invocation. v1 takes a snapshot at picker-open; stale-at-save surfaces as a normal IPC error per §4.1. v2 could subscribe to the activities event stream and live-update; requires either daemon mode or a sidecar process feeding rofi via its dynamic-update protocol.
- **Two-prefix visual seam in `assign-workspace`.** §4.4's full-replacement save semantics resolve the *parsing* ambiguity (returned rows = saved set), but the on-screen overlap of our `[x] ` prefix with rofi's own ballot character remains a UX nit. Drops out automatically when rofi PR #1809 lands; otherwise revisit if users complain.

### Other v2 parking lot

- **Daemon mode** (event-stream-driven; required for live activity indicator in panels). Not justified for v1 — per-invocation IPC overhead is sub-millisecond.
- **D-Bus interface** (panel integration via D-Bus instead of CLI calls). Premature; no panel currently consumes it.
- **Save-to-config-on-exit** semantics beyond the explicit `save` subcommand (e.g., auto-save runtime activities at shutdown). Out of scope; the compositor already discards runtime activities at restart per design.
- **Chezmoi / dotfiles-manager integration for `save`.** v1 writes `$XDG_CONFIG_HOME/niri/config.kdl` directly (§3 `save` note). Out of scope for v1 — same as every other niri-ecosystem app that edits its own config. Re-evaluate only if the user-base on dotfiles-managers is large enough that manual `chezmoi re-add` becomes a real friction.
- **No-focused-workspace exit-0-with-diagnostic alternative.** v1 surfaces `MalformedResponse(Server("no focused workspace"))` → exit 65 on the invariant-breach case (see §4.4 "Focused-workspace contract"). If real-world friction surfaces (e.g., compositor transiently emits all-`false` during animations), consider retry-with-delay (50–100ms) or exit-0-with-stderr-diagnostic. The Phase 3.5.5 empty-activities-on-switch precedent (exit 0 with diagnostic) was the closest UX-policy analog, but Q2 specifically distinguished a contract violation (focus invariant breach) from a valid empty state (no activities configured) — the dispositions are different on purpose.

## Appendix C: Deferred Suggestions (review-surfaced parked items)

- **Fork-side `Action::MoveWindowToActivity`** — required by the `move-window` subcommand; not present at the niri-ipc rev pinned in Phase 3.1 (`54aee6582cbfc11b4e69fa8a602cf2653e29df4a`); either a compositor-loop sub-phase lands it before Phase 3.6, or Phase 3.6 absorbs the rev-bump. From review of `e7d6743` (2026-05-14). IPC variant was out of scope for Phase 3.1 (skeleton-only commit); recorded here so Phase 3.6 planning has a concrete prerequisite to resolve. **Won't fix — superseded (`fa51a5e`, 2026-05-16).** The Phase 3.6 architect spec resolved that `Action::MoveWindowToActivity` never exists by design (compositor DD §4.3 workspace-as-atom model); `move-window` composes `Action::MoveWindowToWorkspace` against the destination activity's trailing-empty workspace instead. The §3 `move-window` DD section was rewritten accordingly.

- **Typed source carriers on `CliError` variants** — Convert `SocketUnavailable(String)` → `SocketUnavailable(io::Error)` and `MalformedResponse(String)` → `MalformedResponse(serde_json::Error)`. Rationale: encodes the error producer in the type so a future call site cannot accidentally route the wrong error class through the wrong variant. Cost: every call site that constructs these variants in Phase 3.2+ must take the typed source rather than stringify. From review of `e7d6743` (2026-05-14). Fixer escalated rather than folded because the shape change propagates through every future call site — architect decision. Consider as Phase 3.1.1 polish before Phase 3.2 wires the first IPC call, or accept the stringly-typed contract permanently. **Partially landed: 2026-05-14 (`38c2f18`).** `SocketUnavailable(io::Error)` and `MalformedResponse(MalformedResponseSource { Decode(serde_json::Error), Server(String) })` landed in Phase 3.2; `MalformedResponseSource` is a two-variant enum rather than the originally proposed bare `serde_json::Error`, to distinguish compositor-side `Err(String)` from wire-decode failure. The "struct variants for all five `CliError` variants" sibling entry remains open.

- **Struct variants for `CliError`** — Convert `String`-shaped variants (`Usage`, `MalformedResponse`, `ActivityNotFound`, `SocketUnavailable`, `CantCreate`) to struct variants with named fields (e.g., `Usage { message: String }`). Rationale: all five variants are type-interchangeable today, so a copy-paste error producing `CliError::Usage("connect refused: ...")` compiles cleanly and exits 64 instead of 69; named-field syntax makes the wrong choice visible in review. Cost: every construction site updates. From review of `e7d6743` (2026-05-14). Bundle with the typed-source-carriers entry above if both are accepted.

- **`map_exit` chain-walk doc trim** — The `socket_unavailable_survives_context_wrap` test (in `src/error.rs::tests`) and the `map_exit` rustdoc both reference "chain walk" as the pinned contract. anyhow's `downcast_ref` already walks the chain, so the test pins the observable contract (`CliError` survives `.context()` wrapping → correct typed exit code), not the iteration strategy. Trim "chain-walk" wording to "context-wrap survival" the next time these comments are touched. From review of `e7d6743` (2026-05-14). Non-blocking — the contract pinned is correct, only the description of the mechanism is imprecise.

- **`variant_name` forward-compat for unknown `Response` variants** — `variant_name` returns `"Response::<unknown>"` for any `Response` variant outside the currently enumerated 16, dropping the variant identity entirely. Extracting the actual variant name from `format!("{r:?}")` would require changing the return type from `&'static str` to `Cow<'static, str>`, with cascading impact on `wrong_variant` and its callers. From review of `8fb13cf` (2026-05-14). Defer to a future sub-phase or to whenever the next non-mechanical touch lands on this code.

- **`serde_json::to_string(&req).expect(...)` in `src/ipc.rs`** — pre-existing pattern (predates Phase 3.3), but Phase 3.3 made it load-bearing as the first production consumer of the IPC layer. The justification "Request serialization is infallible" is wrong in principle — a future upstream `niri-ipc` change introducing a fallible `Serialize` path would turn this into a production panic. From review of `8fb13cf` (2026-05-14). Convert to `?` + new `IpcError::EncodeRequest(serde_json::Error)` variant when the next IPC-layer touch happens.

- **CSV escaping for activity names containing commas** — `--format=<spec>` output silently corrupts CSV when activity names contain commas. Documented in code as out-of-scope for v1; user contract is "activity names don't contain commas." From review of `8fb13cf` (2026-05-14). If the v1 stance changes, implement RFC 4180 quoting (~10 lines).

- **`CliError::OutputPipeClosed` Display message** — currently `"stdout pipe closed"`. Under the current call graph this variant is unreachable in production (the `main.rs` suppression always fires first), but if a future code path reaches `map_exit` with this variant a more explanatory message would help. From review of `8fb13cf` (2026-05-14). Batch into the next `list.rs` / `error.rs` touch.

- **`classify_write_err` vs. `is_stdout_pipe_closed` error-traversal asymmetry** — `classify_write_err` uses `root_cause()` while `is_stdout_pipe_closed` uses `chain().any()`; currently symmetric in behavior because no write site has intermediate non-`io::Error` wrappers. If a future render fn wraps `io::Error` in a non-`Error` type, `root_cause()` would still traverse via `source()`. From review of `8fb13cf` (2026-05-14). Trivial defensive note — consider symmetrising to `chain().any()` in `classify_write_err` on the next touch.

- **Typed `Reply::Err` variants on the compositor side** — every action-subcommand sub-phase (3.4, 3.6) currently substring-matches a wire string (e.g., `msg == "activity not found"`) to route to a typed `CliError` variant. A future fork-side change to `DoActionError` that surfaces a structured error code — rather than `format!("{}")`-stringified text — would let the CLI drop the string-match scaffolding entirely and match on discriminant instead. This is a cross-loop coordination point: the compositor loop would need to introduce a structured error type in the IPC layer, and the CLI loop would consume it via a rev-bump sub-phase. From review of `8ae7384` (2026-05-14). Deferred because the fork's IPC is not yet stable enough to commit to a structured error-code shape. **Phase 3.6 (boxes 1, 3) adds two more wire-string match sites:** `"activity not found"` and `"workspace not found"` in `move_workspace.rs`; `"activity not found"` defensive in `switch_previous.rs`. **Phase 3.6 (boxes 4, 5) adds two more wire-string match sites in `create.rs`:** `"activity name already exists"` (→ `CantCreate`) and `"activity name must not be empty"` (→ `Usage`); `remove.rs` reuses `send_expect_handled`'s existing `"activity not found"` arm. The case for compositor-side typed errors strengthens with each landing.

- **I1 — `CliError::PickerUnavailable` variant** — Picker failures (fuzzel not on `$PATH`, spawn errors) currently route through `CliError::SocketUnavailable`, producing Display output `"niri socket unavailable: fuzzel: command not found..."` — the prefix misnames the cause. The fixer read §4.1's contract as "exit code 69 with binary name in stderr" (exit-code contract, not variant-count contract) and judged the Display lie acceptable for Phase 3.5; minting `PickerUnavailable(io::Error)` sharing exit 69 would fix the Display while honoring the code contract. Architect decision needed: mint new variant or accept misleading prefix? From review of `b11e7eb` (2026-05-14). Deferred because the variant-count / Display trade-off is a design question, not a mechanical fix. **Closed by `5a50ef0` (Phase 3.5.5 landing).**

- **C2 — `is_executable_file` swallows non-`NotFound` metadata errors** — `is_executable_file` returns `bool`, collapsing `PermissionDenied`, `EIO`, `FilesystemLoop`, and other `io::Error` kinds into "not executable" and silently walking past them. Fixing properly requires `Result<bool, io::Error>` and threading the error through `which_in_path` and `ensure_available`. Architect to decide whether the added complexity is warranted given that `EIO`/`EPERM` on `$PATH` entries is rare in practice. From review of `b11e7eb` (2026-05-14). Deferred — correct fix requires a non-trivial signature change across the `which_in_path` call chain. **Closed by `5a50ef0` (Phase 3.5.5 landing).**

- **I4 — Empty-activities interactive UX** — `run_picker` currently exits silently when the activities list is empty (per `run_picker_empty_activities_short_circuits_silently` test). Reviewer argued silent zero-case is bad UX for interactive `switch` (vs. `list`, which is a data pipe). Architect call: emit `eprintln!("niri-activities: no activities configured")` + exit 0, or keep silent? From review of `b11e7eb` (2026-05-14). Deferred as a UX policy question — the current behavior is unambiguously specified; changing it requires architect sign-off. **Closed by `5a50ef0` (Phase 3.5.5 landing).**

- **`tests/picker_shim.rs` — cancel-path shim-invocation observability** — The cancel-path test (`fuzzel_cancel_exits_zero`) cannot distinguish "exit 0 from picker cancel" from "fuzzel never invoked" because the shim's non-zero exit is indistinguishable from no invocation in the assert_cmd output alone. A shim-invoked marker file (written unconditionally at shim start, checked by the test) would close this gap. From review of `b11e7eb` (2026-05-14). Low priority — the happy-path and sidecar-capture tests (`5fb02c7`) already confirm fuzzel is invoked; the cancel-path gap is a negative-space coverage nit.

- **`tests/picker_shim.rs` — `pick_one` stdin-pipe-missing branch is genuinely unreachable** — The `pick_one` error branch for "stdin pipe missing after spawn" is unreachable on a healthy kernel (spawning with `Stdio::piped()` always yields a pipe handle or a spawn error). A code comment documenting the pipe-buffer assumption would make the branch visibly intentional. From review of `b11e7eb` (2026-05-14). Flag for the next `src/picker.rs` touch — trivial one-liner.

- **`src/switch.rs` — `names_focused_first` clones every name** — `names_focused_first` builds a `Vec<String>` by cloning all activity names to reorder them. At O(10) activities the allocation is negligible, but a `&[&str]` signature would avoid the copies. From review of `b11e7eb` (2026-05-14). Low priority; revisit if the function is ever on a hot path or if the signature is refactored for other reasons.

- **`src/picker.rs` — `PICKER_MISSING_MESSAGE` visibility tightening** — `PICKER_MISSING_MESSAGE` is declared `pub(crate)` but no test imports it directly; current shim tests assert on the binary's stderr substring rather than the constant. Either tighten one shim-test assertion to reference the constant (making the pub(crate) load-bearing), or drop the visibility to `pub(super)` / private. From review of `b11e7eb` (2026-05-14). Cosmetic — batch into next `src/picker.rs` touch.

- **`src/picker.rs` — `running_as_root()` checks rUID and EUID defensively** — The chmod-000 permission-bypass skip guard reads `/proc/self/status` and uses `.take(2).any(|uid| uid == "0")` to skip the test if either real UID or effective UID is zero, covering sudo-invoked sessions where real ≠ effective UID. Function name `running_as_root()` implies a single check; the double-check is defensively broader. Future root-skip guards on similar chmod-000 tests should replicate this pattern. From review of `5a50ef0` (2026-05-14). Low priority — no user-facing impact; purely a test-harness pattern note.

- **`tests/picker_shim.rs` — sentinel-file shims must use shell builtins under `env_clear()`** — Integration tests that run shim binaries with `env_clear()` (clearing `$PATH`) cannot call external binaries like `touch` inside the shim script. Use POSIX builtin `: > "$file"` (null-command redirection) instead. Pattern established in `run_picker_empty_activities_warns_and_exits_zero` — the sentinel file tracks whether the shim was invoked at all, closing the cancel-path observability gap noted in the prior `picker_shim.rs` entry above. From review of `5a50ef0` (2026-05-14). Pattern note for future shim-based tests.

- **`src/picker/multi_select.rs::SentinelNames` type-design polish (TD-1)** — `SentinelNames` is currently a struct with two `String` fields holding the matched-pair sentinel names (unicode canonical or underscore-fallback). Convert to a two-variant enum (`SentinelNames::Unicode` / `SentinelNames::Underscore`) with `select_all()` / `only_one()` `-> &'static str` accessor methods. Zero allocation in the no-collision case; matched-pair invariant becomes structural rather than doc-enforced. Reviewer rated current shape 2/5 on invariant expression and 1/5 on enforcement. Mechanical refactor; no behavior change. From review of `048cdba` (2026-05-14). Deferred — no behavior change; the current shape is doc-correct and the collision-guard path is a corner case.

- **`src/assign_workspace.rs::workspace_activity_names` lifetime polish (TD-3)** — currently returns `HashSet<String>` allocating one `String` per membership entry. Could return `HashSet<&'a str>` borrowing from the `&[Activity]` slice, saving N allocations per call. Threads cleanly into `multi_select::build_input_payload`'s `contains` check via `String: Borrow<str>`. Non-hot path; purely a polish item. From review of `048cdba` (2026-05-14). Deferred — O(10)-activities allocation is negligible; revisit if the function is ever on a measured hot path.

- **cli-implementer "before commit" checklist additions** — Phase 3.5b review surfaced two recurring patterns worth lifting into the agent definition's checklist: (1) when two adjacent `?` sites in the same function share the same `.context(...)` string, differentiate them (setup-vs-operation: `"verifying X availability"` vs. `"running X"`); (2) when adding a sibling module to an existing one, grep the existing module's docs for bare "the [module-name]" qualifiers and disambiguate before landing. Currently a post-landing finding only — not enforced. Consider lifting to the agent prompt at a later date. From review of `048cdba` (2026-05-14). Deferred pending a deliberate agent-definition update session.

- **README runtime-deps section pending** — Phase 3.5b deliberately did not add a "Runtime dependencies" stanza to `README.md` documenting that the CLI depends on both `fuzzel` (single-select pickers) and `rofi` (multi-select picker for `assign-workspace`). The implementer cited a no-root-md-files rule; the code-reviewer ruled this defensible-scope-boundary. v1 release prep should add this stanza before any user-facing announcement — a first-time `cargo install`-er hitting `assign-workspace` will get a precise `"picker unavailable: rofi: ..."` diagnostic but no signpost for which picker to install or how the two pickers compose. From review of `048cdba` (2026-05-14). Not blocking for Phase 3.5b; flag for the v1-release sub-phase (Phase 3.7) whenever that lands. **Closed by `23ec8ee`** (README rewrite documents both fuzzel and rofi as runtime deps with install hints, diagnostic-mapping note, and a clarifying entry for the assign-workspace 'Only one…' fuzzel chain that the original spec did not surface).

- **`send_expect_activities` 4-way duplication** — `send_expect_activities` (fetch the activity list from `Request::Activities` and surface `WrongVariant` on mismatch) is duplicated across `src/switch.rs`, `src/move_workspace.rs`, `src/list.rs`, and `src/assign_workspace.rs`. Extraction into `ipc_helpers` stabilises naturally when a fifth call site lands (the `move-window` subcommand in Phase 3.6 box 2). From review of `67097d1` (2026-05-15). Deferred: extraction at four sites would not reduce duplication meaningfully yet; fifth site makes the pattern canonical.

- **`eprintln!` panic-on-stderr-write-failure** — `eprintln!` in `src/switch.rs:105`, `src/assign_workspace.rs:69`, and `src/move_workspace.rs:95` panics if stderr is closed (the macro calls `write!` and panics on error). This matches Rust's standard `eprintln!` contract and the exposure is very narrow (stderr closed at the OS level during a user-interactive invocation is essentially theoretical). Pre-existing pattern established before Phase 3.5; the current sub-phase adds two more sites. From review of `67097d1` (2026-05-15). Low priority; revisit if a future PR consolidates all `eprintln!` sites or if a stderr-propagation story emerges from real usage.

- **`#[allow(dead_code)]` audit on `CliError` variants** — `CliError::CantCreate` gained its first production constructor in `f0b22e8` (`src/create.rs`); the blanket `#[allow(dead_code)]` suppression on that variant in `src/error.rs` should now be lifted. Remaining `CliError` variants under the same suppression should be audited to determine which are still unreachable and which have production constructors silenced only by the allow. From review of `f0b22e8` (2026-05-15). Batch into the next `src/error.rs` touch; not a blocker for landing box 6 (`save`).

- **`src/save.rs` — `AppendOutcome` named-field refactor** — `AppendOutcome::Added(String)` could be `AppendOutcome::Added { content: String }` for call-site clarity (the single positional field's role is not immediately obvious at construction sites). Pure refactor; no behavior delta; spec did not ask. From review of `0a5a5b8` (2026-05-15). Deferred — the positional shape is unambiguous in context; batch into the next `src/save.rs` touch if the enum gains additional variants.

- **`src/save.rs` — `dispatch_reload` IPC-helper consolidation** — `dispatch_reload` duplicates approximately 12 lines that are already available via `ipc_helpers::send_expect_handled` (connect, send, map `Reply::Err` to error). The divergence is that `dispatch_reload` dispatches a `Request::Action` rather than an action-via-helper, and emits a reload-specific stderr breadcrumb. Light-duplication cleanup; not a defect in current form. From review of `0a5a5b8` (2026-05-15). Deferred — the per-breadcrumb stderr output in `dispatch_reload` is distinct enough from `send_expect_handled`'s error path that a clean consolidation would require a context-string parameter; defer until a third similar call site appears.

- **I3 — `Reply::Err` mismapped as `MalformedResponse(Server)` in reload path** — the spec chose `MalformedResponse(Server)` for the `Reply::Err` arm in `dispatch_reload`, but the reviewer argues this misnames the failure: the wire format was correct; the *operation* failed. A new `CliError::ReloadFailed(String)` variant with its own exit code (or sharing 73 alongside `CantCreate`/`ConfigEdit`) would name the failure mode precisely. Touches the typed-error contract and exit-code mapping; affects `src/error.rs`, `src/save.rs`, and the §4.1 table. From review of `0a5a5b8` (2026-05-15). Escalated as a Phase 3.x architectural item; raise to the architect when expanding `CliError` for the next subcommand.

- **TD1 — `CliError::ConfigEdit(io::Error)` carrier conflates three failure modes** — `ConfigEdit` uses `io::Error::new(InvalidData, …)` to encode KDL parse failures and `io::Error::new(NotFound, …)` for missing home dir; filesystem errors arrive as native `io::Error`. A `ConfigEditSource { Io(io::Error), KdlParse(kdl::KdlError), HomeUnresolved }` enum would let callers and stderr output distinguish the three without inspecting `io::ErrorKind`. From review of `0a5a5b8` (2026-05-15). Escalated as a Phase 3.x structural refinement; defer unless similar carriers proliferate or a future sub-phase needs to distinguish the arms programmatically.

- **F11 — `move-workspace <name>` smoke gap** — `move-workspace` is listed as landed in `67097d1` and not in the explicit smoke out-of-scope list, yet no smoke test covers it. Suggested shape: create two `__nact_smoke_mvws_*` activities under `RuntimeActivityGuard`, switch to activity A, capture the focused workspace id via `niri msg --json workspaces`, run `niri-activities move-workspace B`, assert the workspace id appears in B's `workspaces` field and is absent from A's. From review of `9479824` (2026-05-16). Omission is accidental rather than deliberate; the test would follow the same guard + round-trip pattern already established by `smoke_create_then_remove`.

- **F12 — `smoke_list_succeeds` cross-check gap** — `smoke_list_succeeds` asserts exit 0 and non-empty stdout but does not verify the output against `niri msg --json activities`, so the smoke contract "side effect visible via niri IPC round-trip" is not actually exercised for the plain `list` path. Either expand to parse rendered names and assert each appears in `niri msg --json activities`, or fold `smoke_list_succeeds` into `smoke_list_json_parses` and explicitly document that the `--json` variant is the full round-trip check and the plain-text path tests formatting only. From review of `9479824` (2026-05-16). Not a correctness defect in the current code; the gap is in test contract vs. test behaviour.

- **F13 — `smoke_switch_round_trip` silent skip when fewer than two activities exist** — the test emits `eprintln!("smoke: SKIP …")` and returns early when the live compositor has fewer than two activities, leaving no signal in `cargo test` output about whether the test actually exercised the switch path. Fix: make the test self-sufficient by creating two `__nact_smoke_swtch_*` activities under `RuntimeActivityGuard` and switching between those rather than relying on ambient compositor state. From review of `9479824` (2026-05-16). Same pattern already established by `smoke_create_then_remove`.

- **F14 — `smoke_switch_round_trip` focus-leak on mid-test panic** — a panic between the `niri-activities switch other_name` call and the final restore leaves compositor focus on `other_name` with no cleanup. Fix: introduce a `FocusRestoreGuard` that captures the pre-test active activity name on construction and re-issues `niri-activities switch <name>` on `Drop`, parallel to `RuntimeActivityGuard`. From review of `9479824` (2026-05-16). Deferred — the current skip-if-insufficient path limits the window where focus can be leaked; fixing properly requires the F13 self-sufficiency work first.

- **`src/picker/single_select.rs` — `which_in_path_finds_common_shell_utility` test-harness flake under parallel runs** — the test mutates `$PATH` via an env-var write and could race with other `picker/single_select` tests that also read `$PATH` when run under the default parallel test executor. Concretely, the other `which_in_path_*` tests in the same file run in the same process and share the env-var namespace; under `cargo test` parallelism with a narrow thread count the env mutation may be visible to a sibling test mid-run. Fix: either use a per-call `PATH`-override parameter rather than a process-level env-var write, or annotate the test `#[serial]` using the `serial_test` crate. From review of `cb8b573` (2026-05-16). Deferred — flake is environment-dependent and not reproducible in single-threaded test runs; address when the next substantive touch lands on `src/picker/single_select.rs`.

- **`niri-ipc` path-dep → git+rev migration** — `Cargo.toml` currently carries `niri-ipc = { path = "../../niri/niri/gajdusek/niri-ipc" }` (tracked rev: `54aee6582cbfc11b4e69fa8a602cf2653e29df4a`). Switch to `niri-ipc = { git = "https://github.com/gajdusek/niri", rev = "<sha>" }` once the `feature/activities` branch is pushed to a stable remote ref. The path dep assumes the local workspace layout is identical on every build machine; the git+rev form ships everywhere without the cross-repo coupling. Target: `v0.1.1` or the next sub-phase that introduces a rev-bump anyway. From review of `cb8b573` (2026-05-16). Deferred — the path dep is the correct stopgap while the branch is local-only; the migration becomes mechanical once the branch has a stable remote SHA.

- **`Stage2Resolution::NewWorkspace` type-state refactor** — `Stage2Resolution::NewWorkspace` is un-reachable via the picker UI when `include_new_workspace=false`, but the variant is constructible by callers; the `dispatch_stage2` arm for it is `unreachable!()`. A type-state refactor — two distinct `Stage2Resolution` types, one with and one without the `NewWorkspace` variant — would make the constraint structural and eliminate the `unreachable!()`. Requires splitting the enum and threading the type parameter through `dispatch_stage2`. From review of `fa51a5e` (2026-05-16). Escalated as architectural at review; deferred because the current `unreachable!()` fails loudly on any regression and the complexity cost of splitting the type exceeds the gain for a two-caller function. **Closed by `6b7de15` (Phase 3.8 box 1, 2026-05-16).** Type-state refactor landed the variant split as the v0.1.1 unblocker; `unreachable!()` arm structurally eliminated by `Stage2ResolutionLiteralOnly` lacking the `NewWorkspace` variant.

- **`ActiveActivityId(u64)` newtype for structural `NewWorkspace ⇔ active-activity` invariant** — the `NewWorkspace ⇔ active-activity` mapping is currently enforced at `run_picker`'s call-site discipline (active activity → with-new path). Tightening to type-level enforcement would require a newtype `ActiveActivityId(u64)` constructible only via `current_activity_id`, threading it through the with-new pair signatures. Signature changes at ~3 call sites; marginal gain at moderate ceremony cost. From review of `8f06b0c` (2026-05-16). Out of scope for v0.1.1 — type-design reviewer's recommendation was "next sub-phase if pursued, not a fix to this landing." File under type-design polish, post-v1.

- **`move_window.rs` sentinel-struct collapse (`SentinelNames` / `WorkspaceSentinelNames`)** — `src/move_window.rs` carries two `struct`s with a single `String` field each, paralleling the genuine matched-pair `multi_select::SentinelNames` only for naming symmetry; the abstraction does not earn its atomic-pair shape (no two-field invariant to enforce). Escalated as architectural during the Phase 3.6 box 2 (`fa51a5e`) move-window landing review; first-pass resolution at `fa51a5e` was a rustdoc rewrite (acknowledging the symmetry-only motivation) rather than the structural collapse. Three options surfaced: (a) collapse each to `&'static str` flowing through the resolver signatures, promoting the four constants to module-level (recommended — minimal change, removes the dead abstraction); (b) introduce a unified `Sentinel<T>` wrapper used twice (still a too-thin abstraction, plus generics ceremony); (c) keep the rustdoc-only resolution from `fa51a5e`. From review of `fa51a5e` (2026-05-16). **Closed by `2e23edc` (Phase 3.8 box 1b, 2026-05-16).** Option (a) chosen: four module-level `&'static str` constants, six function signatures simplified; option (b) rejected as substituting one too-thin abstraction for another. `multi_select::SentinelNames` (genuine two-field matched-pair counterpart) preserved and cited in module-level rustdoc as the contrasting case.
